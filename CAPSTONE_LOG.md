# CAPSTONE_LOG — M5, вимірювання F1 vs F2

Лог експерименту capstone Module 5: та сама форма наскрізного зрізу робиться двічі —
**F1 руками** (плагін вимкнений) і **F2 через плагін** — щоб побачити, що саме варте
автоматизації і чи справді плагін щось прискорює.

Завдання називає цей файл то `LOG.md`, то `CAPSTONE_LOG.md`. Тут одна назва і один файл:
дві копії неминуче розійшлись би (CLAUDE.md, «ніколи в двох місцях одночасно»).

**Правило дописування:** після кожного коміту в `feat/f1-*` або `feat/f2-*` — рядок у
відповідній таблиці цього файлу (timestamp, крок, метрика). Записується фактом, не
підсумком: якщо промпт довелось повторити тричі — так і пишеться.

**Що таке «вільний промпт»:** будь-яке звернення до агента, написане текстом. Виклик
команди плагіна (`/scaffold-scenario …`) рахується за **один** промпт, скільки б кроків
він усередині не зробив.

---

## Зріз, який повторюється двічі

| Шар | Що з'являється |
|---|---|
| rules | блок правил `<slug>.*` у `app/lib/rules/rules.2026.json` із `source_url` + `verified_at` |
| calc | `app/lib/calc/scenarios/<slug>.ts`, `ScenarioId` у `types.ts`, реєстрація в `scenarios/index.ts` |
| adapters | i18n-ключі в `app/lib/i18n/uk.ts` |
| presentation | рядок у порівнянні (`ComparisonTable`) і картка (`ScenarioCard`) |
| tests | гілки логіки, еталон у `benchmark.test.ts` (виведений вручну), рядок у `flow.test.tsx` |
| docs | EVIDENCE (верифікація джерел), SPEC (кількість сценаріїв) |

---

## Етап 0 — риштування

| Час | Що зроблено |
|---|---|
| 2026-08-03 14:26 | Preflight джерел: `zus.pl`, `podatki.gov.pl`, `biznes.gov.pl` віддають контент (`200`). `isap.sejm.gov.pl` — за Incapsula WAF, `curl` отримує `403`; лишається як `source_url` для людини, цифри беруться з ZUS/podatki. Записано в `.claude/rules/environment-limits.md` |

---

## F1 — `umowa zlecenie`, руками (плагін вимкнений)

**Умови:** не викликаються `/scaffold-rule`, скіл `add-source-domain`, агент `explorer`.
Правило `evidence-numbers.md` лишається чинним — вимкнений інструмент, не стандарт якості.

**Старт:** _(перший промпт — timestamp)_
**Зелений тест:** _(timestamp)_
**Разом:** _(hh:mm)_
**Вільних промптів:** _(n)_

### Промпти (дослівно, у порядку подачі)

| # | Час | Промпт | Результат |
|---|---|---|---|
| | | | |

### Ручні перевірки

| Час | Що перевірялось | Команда / спосіб | Результат |
|---|---|---|---|
| | | | |

### Де зашпортнулись

| Час | Що пішло не так | Скільки коштувало | Як виплуталися |
|---|---|---|---|
| | | | |

### Коміти

| Час | Коміт | Крок |
|---|---|---|
| | | |

---

## F2 — `działalność nierejestrowana`, через плагін

**Старт:** _(timestamp)_
**Зелений тест:** _(timestamp)_
**Разом:** _(hh:mm)_
**Вільних промптів:** _(n)_ · **Викликів плагіна:** _(n)_

### Виклики плагіна

| # | Час | Команда / скіл | Що зробив | Чи знадобилась ручна правка після |
|---|---|---|---|---|
| | | | | |

### Спрацювання хука `layer-boundary`

| Час | Що намагались записати | Що хук сказав | Бойове чи контрольоване |
|---|---|---|---|
| | | | |

### Коміти

| Час | Коміт | Крок |
|---|---|---|
| | | |

---

## Порівняння

| Метрика | F1 (руками) | F2 (плагін) | Дельта |
|---|---|---|---|
| Час до зеленого тесту | | | |
| Вільних промптів | | | |
| Повернень/переробок | | | |
| Спрацювань хука | — | | |

**Висновок:** _(заповнюється після F2. Якщо прискорення менше за 2× — так і пишеться,
разом із причиною; за інструкцією capstone це сигнал повернутись на витягування, а не
підправити метрику.)_

---

## Де плагін НЕ допоміг

Вхід для наступної ітерації плагіна — звідси рядки переїжджають у `docs/BACKLOG.md`.

| Крок | Чому плагін не покрив | Що з цим робити |
|---|---|---|
| | | |
