---
description: Log of known environment blockers in this devcontainer — check here before planning work that depends on builds, servers, or external domains
---

# Межі середовища (журнал відомих блокерів)

Читати перед плануванням роботи, що спирається на білд, сервер або зовнішнє
джерело. Дописувати сюди щоразу, як натрапив на сталу межу — щоб наступна сесія
не витрачала час на те саме.

Файл вантажиться в контекст SessionStart-хуком, тож тримаємо його коротким:
розслідування й післямортеми йдуть у [docs/JOURNAL.md](../../docs/JOURNAL.md), тут — чинний стан і обхід.

| Що | Стан | Перевірено | Обхід |
|---|---|---|---|
| `next build` / `next dev`, vitest + jsdom | **Розв'язано.** Не зависання, а повільна ФС: `/workspace` на 9p (диск Windows), ~10 мс на файловий доступ замість 0.02 | 2026-07-24 | `node_modules` і `.next` — у Docker-томах на ext4. Застосування — тільки **Rebuild Container** (Reopen перевикористовує старий) |
| `next build` у контейнері Mike | **Заборонено.** `build` і `dev` ділили `.next`, продакшн-артефакти ламали dev через `Cannot find module './NNN.js'` | 2026-07-28 | Перевірка — `tsc --noEmit` + `npm test` + `npm run test:ui`. Продакшн-збірку валідує Vercel. Діагностика: `ls .next/BUILD_ID` — якщо є, у теці лежить білд; чистити `find .next -mindepth 1 -delete` |
| Свіжий worktree: залежності | **`npm ci` там коштує 377 с (6 хв 17 с), 169 пакетів.** `node_modules` головного репо лежить у Docker-томі (`/workspace/node_modules`), а worktree його не успадковує — на 9p ставиться з нуля. Заміряно на `git worktree add` у `.claude/worktrees/` | 2026-08-21 | Не ставити заново: `worktree.symlinkDirectories` у `.claude/settings.json` (`["node_modules", "mcp/evidence-guard/node_modules"]`) симлінкує їх із головного репо. **Перевірено живим worktree 2026-08-21:** обидва симлінки створюються автоматично, `tsc` після них — 80 с без жодного `npm install`. Сам `git worktree add` — ~11 с на 306 файлів. Пастка: у `.gitignore` патерн має бути `node_modules` БЕЗ слеша на кінці, інакше симлінк не матчиться і висить untracked |
| `npm install <пакет>` у вже провізіонованому worktree | **Зносить симлінк `node_modules` і повертає 9p-блокер.** npm бачить симлінк як не-теку (`npm warn reify Removing non-directory .../node_modules`), видаляє його і ставить дерево з нуля всередині worktree — 7 хв на 172 пакети. Гірший наслідок не час, а те, що `node_modules` знову опиняється на 9p: jsdom-воркер не стартує, `npm run test:ui` віддає 16 тестів замість 17 і `Timeout waiting for worker to respond`. Читається як зниклий тест, а не як зламане середовище — і `verify` падає саме на розбіжності лічильника зі STATE.md | 2026-08-24 | Ставити пакет у спільний том через головний репо: `npm install --prefix /workspace --no-save <пакет>` (package.json головного не чіпається), потім у worktree повернути симлінк: `find node_modules -mindepth 1 -delete`, `rmdir node_modules`, `ln -s /workspace/node_modules node_modules`. Після відновлення `test:ui` — 17 зелених за 12 с замість 80. `rm -rf node_modules` не пройде: `Bash(rm -rf *)` у `deny` |
| Ручний `git worktree add` проти `claude --worktree` | **`.worktreeinclude` і `worktree.symlinkDirectories` при ручному `git worktree add` не застосовуються взагалі** — обидва механізми належать Claude Code, не git. Дерево виходить голе: ні симлінка `node_modules`, ні `.env`, ні `docs/capstones/`. Симптом оманливий: перший же коміт із такого worktree падає гейтом на `npm run verify` з 6 FAIL про неіснуючі `capstones/*.md`, тобто виглядає як зламаний verify, а не як недоукомплектований worktree | 2026-08-21 | Або створювати worktree через Claude Code, або добивати руками одразу після `git worktree add`: `ln -s /workspace/node_modules <wt>/node_modules` і `cp -r docs/capstones <wt>/docs/` |
| Старт `evidence-guard` (і будь-чого на MCP SDK) | **Холодний імпорт `dist/channel.js` — 17,8 с**, бо `mcp/evidence-guard/node_modules` (4420 файлів) на 9p, а не в томі. Виглядає як зламаний сервер: до бінду порту stderr порожній, `curl` віддає `000`. Теплий старт — ~10 с | 2026-08-21 | Чекати бінду **фактом**, а не таймером: `ss -ltn \| grep <порт>` у циклі. Таймаути під 15 с дають хибний діагноз «сервер не працює» |
| `isap.sejm.gov.pl` як джерело даних | **Непридатний, і не через фаєрвол.** Домен в allowlist, але за ним Incapsula (Imperva) WAF: будь-який `curl` (з cookie-jar і `-L` теж) отримує `302` на себе, далі `403` зі сторінкою `_Incapsula_Resource`. Той самий патерн, що в `tax.gov.ua` з Akamai | 2026-08-03 | Цифри брати з `zus.pl` і `podatki.gov.pl` (обидва віддають реальний контент, `200`). ISAP лишається валідним `source_url` для людини — текст устав там канонічний, просто не витягується скриптом |
| Держкалькулятори як еталон для G2 | **Половина є, половини немає.** Складки: `zus.pl/firmy/przedsiebiorco-przeczytaj-wazne/kalkulator-skladki-zdrowotnej` — Liferay-портлет, форма приймає звичайний POST з `curl`, підтримує `periodYear=2026`, віддає число в HTML. PIT: калькулятора немає взагалі — на `podatki.gov.pl` лишились `taryfowy`, `odsetek`, `walut`, а `kalkulator-wynagrodzen-polski-lad` віддає 404 | 2026-08-04 | Складки — скриптувати (не клікати руками); PIT — виводити з норми з посиланням на артикул. Не шукати зниклий калькулятор MF заново |
| `tax.gov.ua` як джерело даних | **Непридатний, і не через фаєрвол.** Firewall пропускає (код `403`, не `000`), але Akamai WAF ріже `curl` навіть із браузерним User-Agent | 2026-07-28 | Не витрачати час: брати ПКУ з `zakon.rada.gov.ua` (`200`, один статичний IP). `tax.gov.ua` — лише як посилання для людини в `source_url` |
| `www.tax.gov.ua` віддає `000` | **Очікувано.** Akamai anycast, ipset наповнюється лише при старті контейнера. Заміряно: IP зротувались за **години** (у ipset `23.47.124.140/.141`, за півдня вже `.135/.145`) | 2026-07-28 | **Rebuild Container**, не дебаг. Але з огляду на WAF вище — переважно не варто й того |
| `cdn.playwright.dev` / `playwright.download.prss.microsoft.com` (браузер для Playwright/`playwright-cli`) | **Той самий anycast-патерн, третій випадок — і нестабільний навіть у межах однієї сесії.** Домени додано в обидва allowlist-файли, `npm run verify` підтвердив синхронність. Перший Rebuild (з кешем) — `000` на всіх запитах. Rebuild **без кешу** — короткий проблиск: `curl https://cdn.playwright.dev/` дав `400` (TLS+HTTP пройшли), але вже за хвилину той самий домен і той самий резолвлений IP (`150.171.109.53`, Azure Front Door) знову стабільно `000` на трьох поспіль спробах, і `npx playwright install chromium` (180с timeout) не встиг завантажити нічого. Тобто не константний mismatch (той був би стабільним), а флап на рівні edge-балансувальника — TCP SYN то доходить, то ні, на тому самому IP. Системного Chrome/Chromium у контейнері нема (`apt-get install chromium` — пакета в кеші нема) | 2026-08-14 | Rebuild (навіть без кешу) не гарантує вікно стабільності. Якщо потрібен живий Playwright-канал — пробувати `npx playwright install chromium` короткими спробами (не одну довгу на 180с) одразу після рестарту, поки триває вікно; якщо кілька спроб поспіль дають `000` — не наполягати, переходити на канал без живого браузера (typecheck + структурне порівняння DOM/класів, скріншот вручну) |
| `tax-navigator-red.vercel.app` (продакшн-URL продукту) | **Той самий anycast-патерн, вже на власному домені.** `tax-navigator-red.vercel.app`, `vercel.com`, `api.vercel.com` є в allowlist обох файлів, `getent` резолвить домен (`216.198.79.67`, `64.29.17.67`), але `curl` до обох IP висить до `--max-time` і повертає `000` — ipset наповнений іншими IP, ніж ті, куди зараз резолвиться Vercel edge | 2026-08-13 | **Rebuild Container**, не дебаг (як `www.tax.gov.ua` вище). Для будь-якого фонового/планового процесу, що мав би опитувати прод (урок 7.5) — рахувати недоступним у цій сесії, не гадати на альтернативний ендпоінт |
| `.claude/settings.json` | Claude Code не редагує власний settings.json (жорсткий класифікатор, не permission-промпт) | 2026-07-18 | Готую блок для вставки, застосовує Mike |
| `~/.claude/settings.json` | Читається один раз на старті сесії | 2026-07-18 | Правки підхоплює лише наступна сесія |
| `.claude/settings.json` — на старті, але не весь | **Межа проходить по ключах, не по файлу.** `hooks`: доданий серед сесії `PreToolUse` не спрацював — `echo TEST=1 > .env.hooktest` пройшов, файл створився (2026-07-30). `permissions`: **підхоплюються серед сесії** — шість `Bash(gh …)` у `allow`, вставлені Mike під час роботи, одразу пропустили `gh pr create`/`gh pr view` без промпту (2026-08-22, урок 9.4) | 2026-07-30 / 2026-08-22 | Хук перевіряти в **новій** сесії; логіку до того ганяти ізольовано (`bash .claude/hooks/test-*.sh`). Дозвіл перевіряти прямо тут — реальним викликом команди, не перезапуском |
| Класифікатор auto-режиму проти `permissions` | **Випереджає правила з `settings.json` — і `ask`, і `allow` — і вердикт його НЕ детермінований.** `gh pr merge` (у `ask`) двічі поспіль повернув `Permission for this action was denied by the Claude Code auto mode classifier`: відмову, а не питання, тобто до правила черга не дійшла. Після Shift+Tab на acceptEdits та сама команда дала нормальний промпт, Mike підтвердив, merge пройшов. Але `npm run verify` — при `Bash(npm run *)` у **`allow`** — отримав ту саму відмову, а за хвилину пройшов зелений **без жодної зміни режиму** (перевірено прямим питанням Mike). Тобто «це режим» пояснює перший випадок і не пояснює другий: класифікатор переоцінює кожен виклик заново | 2026-08-22 | Відмову на команді, яка явно в `allow`, спершу **повторити** — вона може не відтворитись. Відтворилась двічі — пробувати Shift+Tab на acceptEdits. Головне не діагностувати зламане правило чи зламаний скрипт: `Blocked by classifier` не приходить ні від `settings.json`, ні від коду. Ворота, які мають **не пускати**, auto тримає жорсткіше за `ask`; ворота, які мають **питати**, в auto перевіряти не можна — там вони виглядають як глухе `denied` |
| `/code-review` — виклик з-під агента | **Заборонений жорстко, і не лише виклик.** `Skill` повертає `Skill code-review cannot be used with Skill tool due to disable-model-invocation. Ask the user to run /code-review themselves` і окремим реченням забороняє **відтворювати його роботу іншими засобами**. Той самий клас, що `/goal` і `workflow` (клієнтська команда, не tool), але з явним текстом відмови замість порожнього `ToolSearch`. Вбудований `/security-review`, навпаки, викликається через `Skill` нормально — межа проходить по конкретній команді, не по «вбудованих командах» узагалі. Друга пастка того ж проходу: `/code-review` і `/security-review` збирають дифф через `origin/HEAD...`, тож у клоні без `refs/remotes/origin/HEAD` падають з `fatal: ambiguous argument 'origin/HEAD...'` ще до першого рядка рев'ю; вони ж бачать **лише закомічене**, а `pre-commit-gate` не дає закомітити червоне дерево — дефект, що валить тест, до них фізично не доїжджає | 2026-08-22 | `/code-review` набирає Mike сам у сесії; агент готує предмет і чекає. Довга **мовчазна** фаза (sub-task на пошук + паралельні на фільтрацію) читається як зависання — не тиснути Esc щонайменше 60-90 с. `origin/HEAD` виставляється один раз: `git remote set-head origin -a` |
| `claude-code-action` на PR, що править сам workflow | **Джоб зелений, рев'ю не було.** Екшн звіряє workflow-файл у PR із версією на дефолтній гілці й при розбіжності пропускає себе: `Workflow validation failed. The workflow file must exist and have identical content to the version on the repository's default branch`. Крок `Run Claude Code Review` при цьому `success`, коментарів нуль. Захист від підміни workflow у самому PR, не поломка | 2026-08-22 | Судити про рев'ю за коментарями, не за галочкою. Перший робочий прогін — лише після мержу цих файлів у `master`; до того на PR із правкою `.github/workflows/**` рев'ю не буде взагалі |
| Дозволи GitHub App через `gh` | `gh api repos/<o>/<r>/installation` → `401` (треба JWT самого App), `gh api /user/installations` → `403` (`token authorized to a GitHub App`). Тобто перелік трьох дозволів із CLI не дістати ні тим, ні тим токеном | 2026-08-22 | Дивитись на `github.com/settings/installations` (сторінка людини) або судити за фактом: секрет у `gh secret list` + успішний прогін workflow |
| Нові `.claude/agents/*.md` і `.claude/skills/*/SKILL.md` | Підхоплюються **з затримкою**, не миттєво: одразу після створення спавн падає з `Agent type not found`, а `Skill` — з `Unknown skill: <name>`; за кілька хвилин той самий агент/скіл уже доступний у тій же сесії | 2026-07-29 (агенти), 2026-08-05 (скіли) | Не перестворювати файл і не перезапускати сесію — або зачекати, або разово піти через вбудований `Explore` / прогнати протокол скіла командами вручну |
| `gh` CLI | **Запис від 2026-08-03 застарів.** Той `gh` у `~/.local/bin` зник — бінарник ставили руками в старий контейнер, і він не пережив наступний Rebuild (у `Dockerfile` встановлення взагалі не було прописано). З 2026-08-17 `gh` встановлюється через офіційний apt-репозиторій у `Dockerfile` (`cli.github.com`, build-time, поза runtime-фаєрволом) — стандартний шлях `/usr/bin/gh`, у PATH з коробки. `gh auth login` — все одно ручний крок після кожного Rebuild, токен у образ не бейкається | 2026-08-17 | Перевірити `command -v gh` перед використанням, а не покладатись на пам'ять сесії. Якщо порожньо після Rebuild — залогінитись заново, не шукати старий бінарник |
| `core.fileMode` = `false` у цьому клоні | **Git не бачить біт `+x` узагалі.** `chmod +x` перед `git add` нічого не дає: файл їде в індекс як `100644`, і на іншій машині скрипт не запуститься. Наслідок 9p (диск Windows), не помилка налаштування. Знайдено на `.githooks/pre-commit` | 2026-08-21 | Після `git add` нового скрипта — `git update-index --chmod=+x <шлях>` і звірити `git diff --staged --summary` (має бути `create mode 100755`) |
| `Edit`/`Write` по виконуваному скрипту | **Скидає біт `+x`** у робочому дереві. Симптом оманливий: тест скрипта падає з `Permission denied` і exit 126, що читається як зламана логіка. `git diff --summary` при цьому МОВЧИТЬ — у git режим лишається `100755`, розходиться тільки робоче дерево. Прогін через `bash script.sh` маскує проблему повністю | 2026-08-03 | Після кожної правки `.sh` — `chmod +x` і прогін саме як `./script.sh`, а не `bash script.sh`. Знайдено на `scripts/state-checkpoint/draft.sh` |
| `npx @modelcontextprotocol/inspector --cli` (latest, `2.2.0`) | **Зламаний, відтворено двічі поспіль.** `--cli npx -y <mcp-сервер> --method tools/list` падає з `sh: 1: {method:initialize,...}: not found` — child-process намагається виконати сирий JSON-RPC payload як shell-команду замість передачі в stdin процесу-сервера, далі `Connection timed out after 15000 ms`. Не мережа/фаєрвол — сам пакет | 2026-08-17 | Пінити стару версію: `npx @modelcontextprotocol/inspector@1.0.1 --cli ...` (deprecated, security-fixes only, але CLI-режим робочий) — підтверджено на `context7-mcp tools/list` |
| Firewall matching | Матчить по резолвленій IP, не по SNI | 2026-07-18 | Перевіряти `getent` перед додаванням піддоменів — apex не покриває піддомени на спільних CDN |
| Bash-редирект (`> .env`) | Обходить `Edit`/`Write`-permission-деню | 2026-07-24 | Закривається лише OS-рівнем sandbox; свідомо не виправлено |
| `jq`, `ss`, `ip` | Були не встановлені (типові приклади хуків із документації мовчки не спрацьовували). Додані в Dockerfile | 2026-07-24 | Наявні хуки лишаються на `node` — переписувати назад немає потреби |
| Вкладений `claude --plugin-dir <path> -p "/ns:команда"` | **Ненадійний для команд з tool-use.** Тривіальний `-p "reply with pong"` з `--plugin-dir` відповідає за ~9с; та сама команда з реальним `/plugin:command` (наш `scaffold-rule`, без аргументів — Крок 0 має лише зупинитись і спитати `rule_id`, без tool-виклику) висіла >130с тричі поспіль без жодного виводу після попередження про sandbox. `timeout N` навколо не вбиває дочірній `claude`-процес — лишається живим і після спливання. Причина не підтверджена (permission-prompt round-trip у headless без TTY — робоча гіпотеза) | 2026-07-31 | Structural-перевірка (`claude plugin validate <path>`) — безкоштовна, не висить, цим і обмежуватись. Живий виклик tool-use-команди плагіна — тільки інтерактивно (TTY), не з вкладеного headless `-p` у цьому контейнері. Якщо процес завис — `kill -KILL` за PID, `TaskStop` тулу зупиняє лише трекінг у харнесі, не сам OS-процес |
| `uv`/`pytest`/`pip` (курсові Python-демо, напр. `7.2-ralph-loop`, `7.3-goal`) | **Відсутні, і не тільки локально.** `uv`, `pip3`, `python3 -m pip` — усі `command not found`; PyPI не в allowlist (`curl pypi.org` → `000`). `make verify` курсового демо падає на `uv: No such file or directory`, не на очікуваний `ModuleNotFoundError`/`NotImplementedError` (RED-стан з README не відтворюється). `python3` сам по собі є, `pytest`-модуля нема. У `7.3-goal` таргети `demo`/`demo-weak` — чисті `@echo`, без `uv` — ці два прогнати можна | 2026-08-12 | Контейнер заточений під Node/TS-стек продукту, Python не додавали свідомо. Курсові Python-демо оглядати читанням файлів (і прогоном тих `make`-таргетів, що не тягнуть `uv`), не повним `make verify` тут. Якщо колись знадобиться реальний прогін — `uv`/PyPI-allowlist окреме рішення Mike, не мовчазний обхід |
| `/goal` (лекція 7.3) — виклик з-під агента | **Реальна команда цієї версії CLI** (`2.1.220`, підтверджено `strings` по бінарнику: `Goal achieved`, `/goal clear to stop early`, реалізація — `Stop`-хук типу `prompt`), але це **клієнтський slash-command, не tool**: перехоплюється з інпуту людини до того, як дійти до моделі. Жодного tool/MCP для `/goal` нема (`ToolSearch` по `goal` — порожньо), тож агент не може сам "ввести" `/goal` за Mike — лише людина, безпосередньо в інтерактивній сесії. Додатково: `/goal` відмовляє поза trusted workspace і коли хуки заборонені (`disableAllHooks`/`allowManagedHooksOnly`) | 2026-08-12 | Урок 7.3 (прогін `/goal` на собі) виконує Mike сам, вводячи `/goal <умова>` як своє повідомлення в живій сесії — Claude готує умову і задачу заздалегідь, але не може натиснути кнопку замість людини |
| Плагін `ralph-loop` (маркетплейс `claude-plugins-official`) | **Файли є в кеші, але не активний.** `~/.claude/plugins/marketplaces/.../ralph-loop/` фізично існує (команди, `hooks/hooks.json` з реальним Stop-hook), але `enabledPlugins: {}` у `~/.claude/settings.json` — hook не в конвеєрі поточної сесії. Той самий патерн, що рядок вище про `~/.claude/settings.json`: правка підхоплюється лише наступною сесією, тож `/ralph-loop` у ЦІЙ сесії відразу після активації warm-циклу не дасть — Stop-hook фізично не зареєстрований | 2026-08-12 | Увімкнути в `~/.claude/settings.json` → **нова сесія** → тоді `/ralph-loop "..." --max-iterations N`. У поточній сесії живий прогін неможливий жодним із двох шляхів (канонічний `ralph.sh` — дедлок вище, плагін — цей рядок); робочий обхід на цю сесію — вручну виконати роль циклу (перечитати PROMPT.md, зробити крок, перевірити DoD, повторити до ліміту) |
| `workflow` (лекція 7.4, dynamic workflows) — тригер із-під агента | **Той самий клас, що `/goal`.** Слово-тригер у промпті — не інпут моделі, а клієнтська поведінка `claude` CLI: рантайм сам пише `.mjs`-скрипт і показує картку підтвердження, а `/workflows` (фази, лічильники агентів/токенів) — теж клієнтський UI. `ToolSearch` по `workflow`/dispatch/orchestration — порожньо, жодного tool чи MCP нема. Підтверджено артефактом: `/workflow-launch-exec`, викликаний зсередини SDK-сесії, повернув `no pending launch handoff for this invocation (expected when a restored session replays the dispatch turn)` — механізм існує на рівні CLI, але ця сесія не несе live-стан диспетчеризації | 2026-08-13 | Урок 7.4 (реальний прогін workflow) виконує Mike сам у **окремому** термінальному `claude` (не вкладеному через Bash-тул із активної сесії — це інший, вже задокументований дедлок нижче). Підтверджено робочим: Mike відкрив окремий термінал, тригер спрацював нативно, скрипт збережено як `.claude/commands/audit-i18n-safety.md` |
| Вкладений `claude -p ...` (БЕЗ `--plugin-dir`) з реальним Bash-tool-use | **Причина підтверджена, не гіпотеза.** Запуск `claude -p` із-під Bash-тулу вже активної інтерактивної сесії успадковує `CLAUDECODE`, `CLAUDE_CODE_CHILD_SESSION`, `CLAUDE_PID`, і головне — `CLAUDE_CODE_SESSION_ID`, ІДЕНТИЧНИЙ батьківській сесії. Вкладений процес намагається торкнутись того самого session-стану, що й батьківський (checkpointing/лок) — а той сам заблокований, чекаючи на завершення цього-таки Bash-виклику: дедлок на файловому лоці, процес висить у `D` (uninterruptible I/O wait). `timeout N` шле SIGTERM вчасно, але сигнал не міг доставитись, поки D-стан не розв'язався сам — виміряно ~5 хв на реальному `Bash(git log)`-виклику. Без Bash-tool-use (лише `Read`) симптом не проявлявся так гостро. Знайдено при живому тестуванні `scripts/state-checkpoint/` (5.7 SDK) | 2026-08-02 | `env -u CLAUDECODE -u CLAUDE_CODE_CHILD_SESSION -u CLAUDE_PID -u CLAUDE_CODE_SESSION_ID -u CLAUDE_CODE_ENABLE_SDK_FILE_CHECKPOINTING -u CLAUDE_CODE_SUBPROCESS_ENV_SCRUB -u CLAUDE_CODE_ENABLE_TASKS -u CLAUDE_CODE_DISABLE_FEEDBACK_SURVEY -u CLAUDE_CODE_ENTRYPOINT -u CLAUDE_CODE_EXECPATH -u CLAUDE_AGENT_SDK_VERSION -u CLAUDE_AUTOCOMPACT_PCT_OVERRIDE -u CLAUDE_EFFORT -u AI_AGENT` перед `claude -p` (лишити `CLAUDE_CONFIG_DIR` — без нього губиться workspace trust і `.claude/settings.json` ігнорується). Приклад — `scripts/state-checkpoint/draft.sh`. З голого терміналу (реальний сценарій використання таких скриптів) жодної з цих змінних нема — `env -u` там просто нічого не робить |

## Дозволені домени

**PL:** `zus.pl`, `podatki.gov.pl`, `biznes.gov.pl`, `gov.pl`, `isap.sejm.gov.pl`,
`stat.gov.pl`, `eureka.mf.gov.pl` — кожен у варіантах apex + `www`.

**UA (з 2026-07-28):** `zakon.rada.gov.ua`, `tax.gov.ua`, `www.tax.gov.ua`.

Список мусить збігатися в `.devcontainer/init-firewall.sh` і `.claude/settings.json` —
розбіжність валить `npm run verify`.

## Передісторія

Як розбирали «зависання» Next і jsdom, чому перший діагноз (seccomp) був хибний і
чому відкинули переїзд на WSL2 — [docs/JOURNAL.md](../../docs/JOURNAL.md), розділ «Середовище».
