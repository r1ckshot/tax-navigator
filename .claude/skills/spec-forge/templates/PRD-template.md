---
status: Draft            # Draft | Approved
owner: "Mike"
reviewers: []
updated_at: "<YYYY-MM-DD>"
feature_size: "<з .size: XS/S/M+ — рубрика CLAUDE.md «Глибина артефактів = розмір фічі», не generic XS-XL>"
stage: "03"
ticket: "—"
---

# PRD — <slug>

> **Входи (обовʼязкові):** `idea-brief.md` · `CONTEXT.md` (обидва поруч у цій самій теці)
> **Reference-модуль:** `<шлях>` — які патерни звідти використано (типи сутностей,
> іменовані стани, invariants). Якщо `--reference` не передано і канал не
> обрано — «N/A — green-field, патернів для запозичення нема».
> **Інші канали:** перелік обраних на кроці 4 (документація проєкту / knowledge-base)
> або «None — лише CONTEXT + idea-brief».

## 1. Контекст

<!-- 3-4 абзаци.
¶1 Що вирішуємо — конкретна проблема з idea-brief §2, для кого (idea-brief §3).
¶2 Чому зараз — тригер з idea-brief §4.
¶3 Обраний підхід — 1-2 речення, дослівно чи близько до idea-brief §13.
¶4 (опційно) Traceability: reference-модуль, карта архітектури, критик-override.
БЕЗ конкретної технології (БД/брокер/фреймворк/бібліотека) — це територія
architecture-design, не PRD. -->

## 2. Цілі

<!-- 2-3 вимірювані стратегічні результати, списком. Кожен — прояв обраного
підходу (§1 ¶3). Без сирих чисел тут — числа в §7 KPI. -->

- <ціль 1>
- <ціль 2>

## 3. Поза межами

<!-- 3-4 явні non-goals, кожен — одне речення + причина. Джерело: idea-brief §6.
Edit будь-якої US/AC у §7 walk, що розширює обсяг, автоматично додає сюди новий
рядок — не лише статичний список на старті. -->

- <що свідомо не робимо> — <причина>

## 4. User stories

<!-- ≥5 US, без верхньої межі. Ролі — ЛИШЕ з CONTEXT.md глосарія (не вигадувати
`user`/`admin`). **Дельта spec-forge:** якщо роль нова — перевір, чи це не той
самий актор, що вже названий в іншій фічі цього репо (`docs/features/*/
CONTEXT.md`) під іншим іменем; якщо так, NOT-межа глосарія посилається на
спорідненого актора прямо. Формат:

### US-NN: <дія 3-6 слів>
**Як** <роль з глосарія>
**Я хочу** <дія>
**Щоб** <вимірювана вигода>

Кожна US покрита щонайменше одним AC у §5. -->

### US-01: <назва>

**Як** <роль>
**Я хочу** <дія>
**Щоб** <вигода>

## 5. Acceptance criteria

<!-- ≥1 AC кожного з 5 типів покриття (happy/error/authorization/domain
invariant/cross-context), без верхньої межі. Кожна US із §4 має ≥1 AC. Формат:

### AC-NN (US-XX) — <тип покриття>
**Дано** <бізнес-передумови: роль, стан обʼєктів, попередні події>
**Коли** <дія актора>
**Тоді** <спостережуваний бізнес-результат>

AC = що бачить актор, не як система це робить. ЗАБОРОНЕНО: HTTP-дієслова,
URL-шляхи, числові статус-коди, рядки-помилки `[a-z_]+\.[a-z_]+`, фрагменти
JSON, SQL/DB-конструкції.

**Дельта spec-forge (product-safety.md):** ЗАБОРОНЕНО й слова-поради —
«варто», «краще», «рекомендуємо», «слід» — AC описує, що система ЗАПИСУЄ чи
ПОКАЗУЄ, ніколи що вона радить зробити. Той самий self-check-скан, що виловлює
HTTP-дієслова, ловить і ці слова.

**Дельта spec-forge (іменовані стани):** якщо домен-концепт має ≥3 можливих
результати — опиши їх як закритий-але-розширюваний набір іменованих станів
(за зразком §5 AC-03 `rules-change-monitor/PRD.md`), не boolean. Кожен новий
стан, доданий через Edit під час walk-у, звіряється з уже переліченими на
конфлікт. -->

### AC-01 (US-01) — happy path

**Дано** <передумова>
**Коли** <дія>
**Тоді** <результат>

### AC-02 (US-01) — <тип>

**Дано** <передумова>
**Коли** <дія>
**Тоді** <результат>

## 6. Нефункціональні вимоги

<!-- Таблиця, рекомендований мінімум (не стеля). Цілі — ЧИСЛОВІ — без
прикметників («швидко», «надійно»). Невідоме число → TBD з власником+дедлайном
у §8, ніколи не «швидко». **Дельта spec-forge:** типові рядки для цього
стеку (соло, локальні/скриптовані інструменти, без сервера) — не p95/
throughput/99.9% з корпоративного вебсервісу, а те, що реально вимірюється
логом циклу чи ручною вибіркою. Кожне число тут отримує парний рядок у §9. -->

| Аспект | Ціль | Вимір |
|---|---|---|
| Тривалість одного циклу (старт → готовий результат) | ≤ <N хв> | лог циклу |
| Availability (частка циклів без ручного втручання) | ≥ <N%> на ковзному <M>-циклічному вікні | лог циклів |
| Accuracy (частка висновків із дійсним джерелом) | 100% для тих, що це вимагають | автоматична перевірка проти джерела-істини |
| <ще один, специфічний для фічі> | <число> | <вимір> |

## 6.1 Безпека / приватність

<!-- Класифікація даних (public/internal/confidential/regulated + причина
одним реченням). Персональні дані — які саме поля, чи є взагалі. AuthZ/AuthN —
які перевірки прав додаються, без мови endpoint/route (це рівень architecture-
design). 3-5 abuse cases — кожен із бізнес-відповіддю. Вердикт: security
review Required (M+/нова authz-межа/нові PII) чи N/A з конкретною причиною. -->

- **Класифікація даних:** <...>
- **Торкнуті персональні дані:** <...>
- **AuthZ/AuthN вплив:** <...>
- **Abuse cases:**
  - <ризик 1>: <бізнес-відповідь>
  - <ризик 2>: <як приховано/пом'якшено>
- **Security review:** <Required / N/A з причиною>

## 7. Метрики / KPI

<!-- ≥3 KPI, без верхньої межі, кожен baseline → target з таймфреймом.
baseline=0 ОК для нової фічі. baseline=TBD вимагає inline вимірювального плану
+ парний рядок у §9. -->

- **<метрика 1>** — baseline: <...>, target: <... за ... днів>.
- **<метрика 2>** — baseline: <...>, target: <...>.
- **<метрика 3>** — baseline: <...>, target: <...>.

## 8. Відкриті питання

<!-- 2-4 пункти з `save_as_oq`-резолюцій кроку 7 + нерозвʼязані питання з
інтервʼю. Кожен — з власником і дедлайном; самотнє «TBD» без обох — антипатерн
(даунгрейд до Drop). Формат:
`- [ ] <питання>? Дефолт зараз: <X>. — власник: <імʼя/роль>, до: <дата чи стадія>` -->

- [ ] <питання>? Дефолт зараз: <...>. — власник: <...>, до: <дата чи стадія>

## 9. Evidence ledger

<!-- НОВА секція, якої нема в write-prd. Один рядок на кожне число з §6 і §7
(без винятків — крок 6 не випускає чернетку з незадокументованим числом).
Формат: `<число, дослівно як у §6/§7> — джерело: <файл/скрипт/лог/розрахунок>`.
Мета — той самий інстинкт, що вимагає evidence-numbers.md для rules.2026.json:
жодна цифра не «зі стелі», кожна простежується до конкретного місця, яке можна
перевірити. -->

- <число з §6 чи §7> — джерело: <...>
- <число з §6 чи §7> — джерело: <...>
