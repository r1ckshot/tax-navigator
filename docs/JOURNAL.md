# JOURNAL — як ми до цього дійшли

Append-only хроніка: розслідування, післямортеми, історія сесій. **Не** в
обов'язковому читанні нової сесії — сюди заходять, коли треба зрозуміти, чому
щось влаштоване саме так, і чому відкинули очевидну альтернативу.

Розподіл жанрів:
- **[STATE.md](STATE.md)** — де ми зараз (одна сторінка, перечитується щосесії).
- **[DECISIONS.md](DECISIONS.md)** — що вирішили і чому (рішення, які переживають diff).
- **JOURNAL.md** (цей файл) — як до цього дійшли: заміри, хибні гіпотези, хроніка.

---

# Середовище

## Чому «зависали» Next і jsdom (розібрано 2026-07-24)

**Це була не блокуюча операція, а повільна файлова система.** Попередня версія
запису звинувачувала seccomp — **діагноз був хибний**, лишаю як застереження
проти висновків із надто коротких таймаутів.

Вимір `stat`+`read` на 400 файлах:

| Шлях | stat | read |
|---|---|---|
| `/workspace` | 9.63 мс/файл | 10.43 мс/файл |
| `/tmp` (overlay контейнера) | 0.01 мс | 0.02 мс |
| `/usr/local/lib/node_modules` | 0.02 мс | 0.13 мс |

`/proc/mounts` показує причину: `C:\ /workspace 9p … aname=drvfs`. Тека проєкту
живе на диску Windows і ходить у контейнер через 9p — кожен файловий доступ
приблизно в 500 разів дорожчий.

Арифметика сходиться: `require("jsdom")` тягне **3615 модулів × ~22 мс = 80.6 с**
(доведено інструментованим `Module._load`, який дорахував до кінця). Дефолтний
таймаут воркера vitest — 60 с, тому jsdom не «не стартує», а не встигає. `next dev`
з тієї ж причини виглядав мертвим.

**Контрольний прогін на ФС контейнера** (копія проєкту в `/tmp`, `npm ci` — 18 с):

| Команда | На `/workspace` | На `/tmp` |
|---|---|---|
| `next dev` | нескінченно | **Ready за 2.3 с**, `/` і `/questionnaire` → 200 |
| `npm run test:ui` | таймаут воркера | **10 тестів за 5.8 с** |

Раніше «виключені» гіпотези (sandbox, породження процесів, IPC, мережа, `.next`,
нативний SWC, Turbopack) виключені правильно — просто жодна з них і не була
причиною. Пункт «той самий застосунок висне і в `/tmp`» був хибним виміром:
застосунок лежав у `/tmp`, а `node_modules` резолвився з `/workspace`.

**Обране рішення: том для `node_modules`.** `devcontainer.json` монтує named-volume
`tax-navigator-node-modules` на `/workspace/node_modules` (ext4), `Dockerfile`
готує теку від root, щоб свіжий том належав `node`, `postCreateCommand: npm ci`
його наповнює, `WATCHPACK_POLLING=true` рятує HMR (inotify через 9p не працює).
Вихідники лишаються bind-ом на `C:\` — їх мало, 9p на них терпимий.

Заміри після Rebuild Container: `test:ui` 60 с (холодний кеш) → **12 с** (теплий,
кеш vite теж на томі); `next dev` **Ready 6.4 с**. Перший прогін будь-чого
повільний через холодний кеш — це разово, не баг.

**Відкинуто: переїзд репо на ext4 WSL2.** Чистіше в теорії, але щоразу впиралось у
reattach контейнера до старої `C:\`-копії, і коштувало б втрати редагування з
Windows. Том дає те саме прискорення без цього. Якщо колись 9p на вихідниках
почне муляти (важкі git-операції) — тоді переїзд, але це вже комфорт, не блокер.

## Справжня причина краху `Cannot find module './NNN.js'` (2026-07-28)

Спершу списали на 9p і винесли `.next` у named-volume `tax-navigator-next` — том
корисний (ext4 замість 9p пришвидшує dev-кеш), але **краху він не лікував**.

Реальна причина: `next build` і `next dev` ділили одну теку `.next`. Claude багато
разів ганяв `npm run build` для перевірки в тому ж контейнері, де Mike потім
відкривав `npm run dev` — dev спотикався об артефакти продакшн-збірки
(`server/pages/_document.js`, `webpack-runtime.js` з посиланнями на неіснуючі в dev
чанки).

Виправлення: `distDir: process.env.NEXT_DIST_DIR || '.next'` у `next.config.mjs`
(Vercel без цього env бере дефолт) + політика «Claude не білдить у контейнері
Mike». Виявилось, що навіть `NEXT_DIST_DIR=.next-verify` частково протікає в
`.next`, тож надійніше просто не білдити тут. Після чистки `find .next -mindepth 1
-delete`: `next dev` Ready 2.7 с, `/` і `/questionnaire` → 200 без module-error.

---

# Хроніка сесій

## 2026-07-16/17 — M3 capstone, ініціалізація репо
Здано (`docs/capstones/m3.md`). Три баги знайдено вже після здачі, при реальному
відкритті devcontainer — записані в DECISIONS 2026-07-17.

## 2026-07-18 — Next.js scaffold, Vercel, звірка P1-matrix
- Репо: Next.js (TS, App Router), деплой на https://tax-navigator-red.vercel.app
- Firewall: 11 офіційних PL-доменів у `init-firewall.sh` (Claude) + `settings.json`
  (Mike руками) → Rebuild Container, `npm run verify` зелений.
- **P1-matrix звірено проти gov.pl / zus.pl / isap / інтерпретацій KAS.** Ядро
  підтверджено (zdrowotna пороги, duży ZUS 1,926.76, ryczałt 12%, liniowy 19% +
  14,100, skala, nierejestrowana). Виправлено: спецнорма резидентства = art. 52zr
  із сансетом 31.12.2026; preferencyjny 456.18 (було ~456.19). Уточнено: były
  pracodawca — два різні тести; мін. zdrowotna 432.54 з лютого 2026; 50% KUP ліміт
  120k; KSeF для JDG з 01.04.2026. Кожен сценарій має рядок «Верифіковано» + source_url.

## 2026-07-24 — анкета-навігатор (движок + UI)
- **Бракуючі цифри звірено** й дописано в EVIDENCE (сценарій F): внески роботодавця
  (9.76 + 6.50 + 1.67 + 2.45 + 0.10 ≈ 20.48%), працівника (13.71% + zdrowotna 9%),
  KUP 250/міс, kwota zmniejszająca 300/міс, 30-krotność 282,600 (= 30 × 9,420).
- **ЄСВ/ВЗ звірити не вдалось** — укр-домени поза allowlist. Рішення: сценарій ФОП
  без числового діапазону, ризик 🔴 + цитати KAS. (Знято 2026-07-28, див. DECISIONS.)
- **Движок** (`app/lib/`): rules-as-data `rules.2026.json`, резидентство (183 дні /
  дві осі центру інтересів / art. 52zr / тай-брейки Конвенції по черзі), ZUS з явним
  пріоритетом (zbieg → історія 60 міс → етап), сценарії ФОП / JDG / інкубатор / UoP.
- **Анкета:** 10 екранів, ~14 полів, умовний хвіст. Екран ≠ поле: центр інтересів
  питається двома осями, бо норма — «особистих АБО економічних».
- **Приватність:** точна виручка живе лише в пам'яті — не йде ні в sessionStorage,
  ні в share-лінк.
- **Тести:** 71 node + 10 UI.

## 2026-07-24 (друга частина) — процес
- **Правила винесені у файли** (`.claude/rules/`): preflight, testing, visual-review,
  evidence-numbers, environment-limits. Підключені в CLAUDE.md.
- **Hooks** підготовлені й пайп-протестовані → `.claude/hooks.suggested.md`.
  Виявлено: `jq` у контейнері немає, тож стандартні приклади хуків мовчки не
  працюють — усе переписано на `node`.
- **EVIDENCE §6: калібрувальний приклад перераховано движком.** Стара цифра UoP
  ~10.7k була від брутто й без 32%-ї ставки; нова — 8,370–9,067 від повного кошту
  роботодавця. Причина розбіжності записана в самому файлі.

## 2026-07-24 (третя частина) — середовище і перший показ
- Розібрано «зависання» Next і jsdom → див. розділ «Середовище» вище.
- **Анкету вперше побачила людина.** Mike пройшов `/questionnaire`; вердикт —
  працює, але не красиво. П'ять зауважень → U7.
- **Виправлено відновлення після F5** (`resumeIndex`): збережений крок став стелею,
  а не істиною — інакше людину висаджувало після порожнього поля виручки.
- Гілка `feat/questionnaire-navigator`: 8 атомарних комітів.

## 2026-07-28 — U7 редизайн UI + фікс dev-крашу
- Редизайн доведено через кілька раундів рев'ю Mike вживу. Фундамент — дизайн-токени
  + один teal-акцент у `globals.css`.
- **Результат:** довгий скрол → одна порівняльна таблиця + акордеони; вердикт у
  нейтральній акцентній плашці (не зелений/червоний — свідомо).
- **Ввід:** усі числові поля стали слайдерами (виручка по кроку 2 500, дні 0–366).
  Квантизація дає приватність + переживання F5 за конструкцією.
- **Анкета:** житло в UA — умовний екран із предиката `homeInUaMatters`, виведеного
  з `residency.ts`, а не вгаданого.
- **Тести:** 71 node + 8 UI (−2 UI: прибрано тести валідації вводу й окремої легенди,
  які втратили сенс після редизайну; додано ryczalt-limit на рівні розрахунку).

## 2026-07-28 (друга частина) — M4.10 на реальному коді
- Завдання адаптовано, а не виконано «в стіл»: 4.10 просить заскафолдити новий
  проєкт із 6 файлів, але саме завдання дозволяє відхилення, а прецедент був у 4.8.
  Три файли з шести вже існували.
- **Дописано:** ARCHITECTURE.md, SPEC.md, ADR-0001 і ADR-0002, блок правила
  залежностей у CLAUDE.md.
- **Правило залежностей стало машинним контрактом:** `.dependency-cruiser.cjs`
  (8 правил) + `architecture.test.ts` (браузерні глобали) — обидва в `npm test`.
- **Кожне правило доведене навмисною поломкою** — і саме так виявилось, що
  `core-no-external` був привидом. Деталі — коміт `6d0d723`.
- **Тести:** 74 node + 8 UI, `tsc` чистий, depcruise 0 порушень на 59 модулях.

## 2026-07-28 (третя частина) — SDLC ideation, фіча `tg-assistant`
- **Пройдено скіл `interview`** (14 фаз): Socratic-інтерв'ю, конкурентний аналіз,
  три стратегічні підходи й три перспективи рев'ю паралельними sub-agent-ами,
  devil's advocate із чистим контекстом, RICE і Feasibility. Плагін `sdlc` не
  встановлений — протокол виконано за `SKILL.md` вручну.
- **Артефакт:** `docs/features/tg-assistant/idea-brief.md` (15 секцій, RICE 3.25) +
  глосарій `CONTEXT.md`. Заглушки на дві інші ідеї — `paid-action-plan`,
  `rules-change-monitor` (status Draft, лише §1).
- **Головний висновок рев'ю:** жоден із трьох підходів не має двох плюсів у матриці,
  а Executive і devil's advocate незалежно вказали на те саме — вузьке місце не в
  даних, а в тому, що питання «купиш?» ще не поставлене достатній кількості людей.

## 2026-07-28 (четверта частина) — розчистка задач і документів
- Відкрито укр-домени, врізано `environment-limits.md`, вставлено хуки (U2/U3).
- Вирівняно фазу курсу в 5 файлах, полагоджено биті посилання, розділено STATE і
  JOURNAL, BACKLOG перебудовано в чергу NOW/NEXT/LATER.
- Причини — DECISIONS 2026-07-28 (третя частина).

## 2026-07-29 — карта архітектури і ЄСВ/ВЗ для ФОП
- Три непов'язані теми спершу жили на одній гілці, розділені заднім числом —
  звідси правило «гілка = одна задача зі STATE» (DECISIONS 2026-07-29).
- `docs/architecture-map.md` + вендорений скіл `map-architecture` і агент `explorer`.
  Перша версія карти мала ~12 хибних `file:line` — усі правдоподібні на око; звідси
  `scripts/check-anchors.mjs` у `npm run verify`.
- ЄСВ/ВЗ для ФОП (`/scaffold-rule fop.esv_vz`): ЄСВ мін. 1,902.34 грн/міс, ВЗ 1%.
  `rangeMonthly` лишився `null` свідомо — складки ZUS для `zakład` не звірені
  (BACKLOG → LATER). Курс UAH→PLN не конвертуємо (DECISIONS 2026-07-29).

## 2026-07-30 → 2026-08-02 — M5 інструментарій (уроки 5.1–5.7)
Хронологія артефактів: скіл `add-source-domain` + бенчмарк (100% проти 85% на
baseline) → хук `block-env-writes` → плагін `tax-navigator-toolkit/` → публічний
[team-marketplace](https://github.com/r1ckshot/team-marketplace) → `pre-commit-gate`
→ SDK-скрипт `scripts/state-checkpoint/`.

Що з цього не видно з коду:
- **Бенчмарк скіла показав дельту 0 проти baseline, поки команда `scaffold-rule`
  переказувала ту саму процедуру** — baseline просто читав команду. Дублі прибрані,
  команда тепер вказує на скіл.
- **`claude plugin validate` — єдина чесна перевірка плагіна в цьому контейнері.**
  Живий `--plugin-dir` виклик tool-use-команди тричі завис (>130 с) — межа записана
  в `environment-limits.md`.
- **CI marketplace був зашитий на одне імʼя тесту хука** і мовчки не запускав би три
  сюїти toolkit-а, лишаючись зеленим. Полагоджено при публікації (Етап 5 capstone).
- **`is_error: false` від SDK не гарантує фактичну точність:** агент кілька разів
  повертав валідний за схемою, але вигаданий контент (неіснуючий Makefile-таргет,
  невірна кількість тестів). Trust-модель «скрипт ніколи не комітить сам»
  підтверджена емпірично — DECISIONS 2026-08-02.
- **Дедлок вкладеного `claude -p`** через успадкований `CLAUDE_CODE_SESSION_ID`
  (~5 хв у D-state) знайдено саме живим прогоном, не читанням доків —
  `environment-limits.md`.

## 2026-08-03/04 — M5 capstone: F1 vs F2, і що показав замір
Повний лог з таймстемпами, промптами й таблицями — [CAPSTONE_LOG.md](../CAPSTONE_LOG.md).
Сюди — висновки, яких у ньому немає як окремого тексту:

- **Мутаційна перевірка знецінила «зелені тести».** Еталони сценаріїв звірялись через
  `rangeContains`, а смуга ±4% ковтає похибку в 164 zł/міс: 3 з 5 навмисних регресій
  проходили тест. Після переписування на центр смуги — 5 з 5 і далі 7 з 7 у F2.
  Решта сценаріїв досі на старій звірці (BACKLOG → LATER).
- **Наївна версія F2 була б неправильною на ~23%.** «Nierejestrowana = без ZUS»
  вірне лише для продажу товарів; за послуги договір є zleceniem за k.c. — складки
  платить замовник. Знайдено при звірці джерел, не при кодуванні.
- **Плагін не рятує від верстки.** Обидва візуальні рев'ю Mike знайшли те, чого не
  бачить жоден тест; головне — 2 850 zł українського тягаря прочитані як «на руки».
  Звідси рішення прирівняти помилку в комунікації числа до помилки в числі
  (DECISIONS 2026-08-03).
- **DoD зірвано двічі за модуль**: коміти хуків замерджені без запису в STATE.md, і
  роботу оголошено завершеною при застарілому NOW. Обидва рази ловив Mike, не гейт —
  тому останній крок DoD у CLAUDE.md сформульований як «перечитати STATE і BACKLOG
  очима наступної сесії».
- Здано 2026-08-04 постом у TG-гілку «Модуль №3».

## 2026-08-05 — M6, урок 6.2: готовий `interview` вручну на `rules-change-monitor` vs власний на `tg-assistant`

Простий рівень (toy-ідея, не закомічено — лише для скріншотів) і Складний рівень
(живий прогін на реальній фічі) курсової вправи. Деталі —
[rules-change-monitor/idea-brief.md](features/rules-change-monitor/idea-brief.md),
[rules-change-monitor/CONTEXT.md](features/rules-change-monitor/CONTEXT.md).

- **Готового (невендореного) `interview`-скіла в репо ніколи не існувало окремо від
  адаптації** — 28.07 команда одразу переписала й закомітила `.claude/skills/interview/`,
  без проміжного «як є з коробки» прогону. Щоб отримати чесну базу для порівняння,
  довелось вручну виконати оригінальний `docs/course/.../plugin/skills/interview/SKILL.md`
  крок за кроком (без встановлення плагіна — `environment-limits.md` фіксує headless
  `--plugin-dir` як ненадійний) на новій фічі, а не перезапускати вже закомічений `tg-assistant`.
- **Найбільша реальна різниця виявилась не в протоколі, а в мові й формулюванні
  питань.** Структура (15 секцій, той самий порядок) ідентична в обох брифах — це не
  дивно, бо власний скіл лише перекладає і дописує стиль, не змінює каркас. Але
  готовий скіл сам собою не змушує писати українською чи пояснювати прихований
  trade-off у кожній опції `AskUserQuestion` — цей текстовий мандат (`ask-style`,
  3-4-речеве питання + 3-5-речева опція з hidden trade-off) є лише у власній версії
  й ніде в оригіналі.
- **Побічна знахідка готового прогону цінніша за сам бриф**: Phase 10 (feasibility
  скан репо) наткнувся на `scripts/check-stale-rules.mjs` + `.claude/hooks/session-stale-rules.mjs`
  — дрейф-детекція для цієї самої ідеї вже існує і ніхто про це не згадував у
  BACKLOG-рядку M10. RICE=12.5 і Approach C (гібрид: автозвірка для всіх безкоштовно +
  платний алерт лише на вже скриптованих джерелах) — прямий наслідок цієї знахідки,
  а не абстрактного мислення.
- Крок готового скіла, який довелось би переробити найбільше при повторному
  використанні: Phase 3 (deferred fix-term до Phase 12/ExitPlanMode) — тут сесія не
  була в plan mode, тож глосарій зроблено окремим bootstrap-кроком ДО interview, як і
  вимагає завдання курсу, а не всередині фаз 1-11.
- **Перше зарахування «власного скіла» було неповним — Mike це впіймав.** Спершу
  прийняв уже адаптований 28.07 `.claude/skills/interview/` як власний скіл цієї
  вправи без перевірки. Реально відрізнявся не лише мовою (це й так було видно), а й
  структурою (`general-purpose` sub-agents, глосарій у теці фічі, гейт коміта, розділ
  уроків) — але завдання explicitly просило ще чотири речі, яких у файлі не було:
  приклади Сократівських питань під нашу аудиторію, приклади глосарію з наших
  продуктів, 3 приклади підходів типових для нашого ринку, sentinel errors/паттерни
  модулів цього репо як reference. Дописано в SKILL.md 2026-08-05, без повторного
  прогону — `tg-assistant`-бриф лишається чесним доказом роботи скіла, просто
  зроблений ДО цієї доробки.

## 2026-08-06 — M6, урок 6.4: `architecture-design` на двох фічах vs власний `sad-forge`

Простий рівень (`tg-assistant`, §1-§5, Edit-стовп із посиланням на §2-інцидент) і
складний рівень (`rules-change-monitor`, перекласифікований S→M саме для цієї
вправи, §1-§12 + повний критик-раунд) — обидва через вендорований
`.claude/skills/architecture-design/`. Деталі —
[tg-assistant/sad.md](features/tg-assistant/sad.md),
[rules-change-monitor/sad.md](features/rules-change-monitor/sad.md) (3 ADR, 6
знахідок критика, усі виправлені).

- **Критик спіймав те, що сама сесія написала й не помітила.** Найважче ADR-0002
  rules-change-monitor цитувало AC-10 як driver, хоча PRD §8 текстом каже: під
  дефолтним дводоменним allowlist AC-10 не спрацює жодного разу (вето-цифра лежить
  у записі поза скопом). Це не було вигадкою критика — рядок уже був у PRD, просто
  ніхто (включно з автором SAD) не звів його з ADR-обґрунтуванням під час самого
  Socratic walk. Так само зловлено самосуперечність у ADR-0003 (Positive каже «ризик
  дрейфу знятий», Negative — «ризик дрейфу, якщо забути») і вигадану роль «Tech
  Lead», якої нема в CONTEXT.md жодної з двох фіч.
- **`rules-change-monitor` дав лише 3 ADR на M-розмірі, не 5-12 «типових».** Свідомо
  не роздуто штучно — кожен border-кейс (пауза між запитами, вибір scripts/-теки)
  чесно не набрав порогу і лишився inline з підтвердженням Mike. Роздування ADR
  заради квоти — той самий антипатерн, що «ADR на кожне рішення», проти якого
  застерігає сам курсовий `checklist.md`.
- **Порівняння `architecture-design` vs власний `sad-forge`** (обидва прогнані на
  `tg-assistant` §1-§5, [sad-sad-forge.md](features/tg-assistant/sad-sad-forge.md)):
  контент вийшов майже ідентичний — той самий C4Context/C4Container, той самий
  `target_surfaces: [worker]`, ті самі три ADR-стовпи §4. Найпомітніший приріст
  якості — §1 Stakeholders: `sad-forge`-шаблон явно забороняє sign-off-роль, якої
  нема в CONTEXT.md, тож вигаданого «Tech Lead» там просто не виникло — саме ту
  помилку, яку в `architecture-design`-прогоні зловив лише пізніший критик, на
  іншій фічі. Другий приріст — §4 стовп 3 (JSON-файл стану): 4-й критерій «зачіпає
  постійне сховище» дав ADR напряму, без розтягування курсового «незворотнє» так,
  як довелось у `rules-change-monitor`. Найбільше довелось би переробити в
  курсовому оригіналі — саме крок 4 (item-bank Target-surface): курсова таксономія
  без прив'язки до жодного репо змушує вигадувати дефолт щоразу заново, тоді як
  `sad-forge` дає прямий lookup («tooling поза app/lib» + «безнаглядно» → `worker`)
  за одну секунду, не питання.

## 2026-08-07 — M6, урок 6.5: `complete-sequence-diagrams` + `generate-data-model` на двох фічах vs власний `migrations-forge`

Простий рівень (`tg-assistant`) і складний (`rules-change-monitor`, повне
закриття 10/10 AC + breaking change + roundtrip) — обидва невендореним
пайплайном, вручну, той самий підхід, що 6.2/6.3/6.4. Деталі — [BACKLOG.md](BACKLOG.md)
таблиця «Курс», рядок 6.5.

- **Порушив власний протокол скіла посеред прогону — і це виявилось цінніше,
  ніж якби все пройшло гладко.** `_shared/diagram-presentation.md` прямо
  забороняє «сирий Mermaid як питання підтвердження» — перше підтвердження
  потоку `tg-assistant` зробив саме так (mermaid у `preview` поля
  `AskUserQuestion`, до запису у файл). Mike запитав напряму «ти зробив як
  скіл каже?» — перевірка проти вихідного файлу, не проти пам'яті про нього,
  зловила розбіжність одразу. Для решти потоків (rules-change-monitor)
  порядок виправлено: файл → валідація → проза → підтвердження. Урок: коли
  сам пишеш «скіл», легко відтворити саме ту помилку, від якої скіл
  захищає, — перевірка нічого не варта, якщо звіряєш із власною пам'яттю,
  а не з референс-файлом.
- **Async-класифікація за ключовим словом працює правдоподібно, поки не
  питаєш «а ЩО саме тригерить цей конкретний потік».** Перший чорновик
  (`rules-change-monitor`, AC-02) застосував idempotency-key/retry/DLQ до
  внутрішнього кроку класифікації лише тому, що ФІЧА в цілому — щомісячний
  cron; це дублювало б уже намальований дизайн ретраю в Потоці 2
  («наступний цикл повторить», не exponential backoff). Правильне правило —
  дивитись, що тригерить САМЕ ЦЕЙ потік: якщо він викликається зсередини
  вже запущеного циклу (як класифікація в rules-change-monitor), він sync,
  навіть якщо вся фіча — async на верхньому рівні. tg-assistant Потік 1,
  навпаки, ДІЙСНО є точкою входу cron — там патерн лягає природно (AC-09
  буквально вимагає дедуп, ідемпотентність не притягнута).
- **`.env`-подібна пастка гейта, що не про мій diff.** Перший коміт упав на
  `check-docs.mjs`: токен `` `tg-assistant` `` у бектиках усередині
  «Закрито 2026-08-06» блоку `STATE.md` збігся з тим самим бектик-токеном у
  живому рядку `BACKLOG.md` NOW — хоча закритий був лише SAD, а не
  `tasks/`. Не чіпав логіку скрипта (він уже раз ловив баг, полагоджений з
  доведенням мутацією) — прибрав бектики з двох місць, де вони позначали
  просто ім'я фічі в реченні, не заявку про закриття.
- **Порівняння `generate-data-model` vs власний `migrations-forge`**
  (обидва на `tg-assistant`,
  [data-model-migrations-forge.md](features/tg-assistant/data-model-migrations-forge.md)):
  сутності, зв'язки й індекси вийшли ІДЕНТИЧНИМИ — обидва скіли читають той
  самий вхід (`sad.md` §6 persist-нотатки), тож розбіжність лише в типах
  колонок (`TEXT`/`INTEGER` проти `UUID`/`TIMESTAMPTZ`/`VARCHAR(N)`/`BOOLEAN`).
  Найбільший приріст якості — не на цій фічі, а на `rules-change-monitor`,
  де курсовий скіл згенерував `ALTER COLUMN SET NOT NULL` для contract-фази
  breaking change, і лише живий roundtrip-тест на `node:sqlite` показав, що
  цей синтаксис у SQLite не існує взагалі (`ALTER TABLE` там — лише
  `ADD`/`DROP COLUMN`/`RENAME`). Найбільше довелось би переробити в
  курсовому прогоні при повторному використанні на цьому репо — саме крок
  13 (breaking-change decomposition): дефолт `ALTER COLUMN` мовчки
  припускає Postgres, і кожен наступний breaking change ламався б так
  само, доки хтось не наткнеться на це руками. `migrations-forge` планує
  table-rebuild одразу, не постфактум.

## 2026-08-10 — M6, урок 6.6: `api-forge` на двох фічах vs власний `contract-forge`

Простий рівень (`tg-assistant`) і складний (`rules-change-monitor`,
reconcile-демо) — той самий невендорений підхід, що 6.3-6.5. Деталі —
[BACKLOG.md](BACKLOG.md) таблиця «Курс», рядок 6.6.

- **Завдання написане під REST-фічу, обидві реальні фічі репо — не REST.**
  `api-forge` вибирає форму контракту з `sad.md` frontmatter `target_surfaces`
  (`_shared/surfaces.md`) — `rules-change-monitor` задекларований `["worker"]`
  ще на 6.4, `tg-assistant` лишив поле порожнім, і PRD прямо каже «без
  публічного API». Обидва — не `backend-service`, тож жоден прогін не дав
  `openapi.yaml` з POST/409, як буквально описує текст лекції (той приклад —
  з `course-lesson-mvp`, не з цього репо). Питання, чи підмінити реальні
  фічі прикладом, вирішив явно Mike: ні — текст лекції згадує
  `course-lesson-mvp` лише для випадку відсутніх вхідних файлів
  (`data-model.md`/`sad.md`/`PRD.md`), а не відсутнього HTTP; у нас вони є.
- **«Skip» — задокументована гілка протоколу, не недороблена робота.** Для
  `tg-assistant` крок 1 каже: немає інтерфейсу → одно-рядкова нотатка в
  звіті, одразу до `break-tasks`, без `openapi.yaml`. Перша реакція на
  результат була «чи ми взагалі виконали завдання» — відповідь: мету
  («відчути механіку api-forge») виконано, буквальний текст («PASS +
  openapi.yaml») ні, і це очікувано для не-HTTP фічі, узгоджено заздалегідь.
- **Codegen-крок хард-рівня не зі скіла, а з тексту лекції.** Курсовий
  `api-forge/SKILL.md` не згадує `oapi-codegen`/`openapi-typescript` жодного
  разу — очікування codegen прийшло з тексту завдання, яке мовчки припускає
  `openapi.yaml`. Без нього (worker → `events.md`) генерувати нема з чого;
  позначено N/A в `api-sync-report.md` з поясненням, не пропущено тихо.
- **Порівняння `api-forge` vs власний `contract-forge`** (обидва на
  `rules-change-monitor`,
  [events-contract-forge.md](features/rules-change-monitor/contracts/events-contract-forge.md)):
  на відміну від `migrations-forge` (де типи розійшлись одразу),
  тут вихід вийшов структурно ІДЕНТИЧНИМ — курсовий `templates/events.md`
  ніколи не мав HTTP-специфічних дефолтів (`BearerAuth`, cursor-пагінація),
  вони живуть лише в `templates/openapi.yaml`, тож на `worker`-поверхні
  `contract-forge`-Defaults просто нема що перевизначати. Реальна різниця
  (auth/pagination/codegen opt-in замість мовчазного дефолту) показала б
  себе лише на гіпотетичній `backend-service`-фічі, якої в цьому репо ще
  немає — чесна, а не притягнута знахідка: не кожне порівняння власного
  скіла з курсовим зобов'язане показати велику різницю.

## 2026-08-10 — M6, урок 6.7 (останній): `sdlc-task-packages` на двох фічах vs власний `tasks-forge`

Простий рівень (`tg-assistant`) і складний (`rules-change-monitor`,
capstone) — той самий невендорений підхід, що 6.3-6.6. Деталі —
[BACKLOG.md](BACKLOG.md) таблиця «Курс», рядок 6.7.

- **Завдання (Stage 1/2/3, waves, `AskUserQuestion`-checkpoint, 8-gate
  таблиця, `context_budget ≤5000`, `--feature=<slug>`) дослівно описує не
  `sdlc/plugin/skills/break-tasks/` (простіший, 13 плоских кроків, без
  хвиль, `docs/features/<slug>/tasks/`), а
  `playbook/skills/sdlc-task-packages/` (3-stage, waves, 8-gate,
  `delivery/<slug>/tasks/`).** Нотатки уроку `docs/notes/6-sdlc.md` §6.7
  теж називають цю хвильову механіку іменем `break-tasks` — плутанина в
  самому курсі, не знайдена цією сесією помилка планування. Вирішено
  заздалегідь, у сесії планування: беремо протокол `sdlc-task-packages`,
  вивід — у `docs/features/<slug>/tasks/`, як в усій попередній серії
  (рішення записане в `DECISIONS.md` 2026-08-10).
- **Перше чорнове групування «1 story на 1 файл» (те, що спрацювало на
  `tg-assistant`) не витримало на `rules-change-monitor`.** `diff.mjs`
  (`sad.md` §5: «присвоює один із 7 станів AC-03, звіряє з veto-списком
  AC-10») виявився не одним нероздільним блоком — його гілки мають РІЗНІ
  реальні залежності: allowlist/verify-гілки Потоку 3 явно кажуть «без
  звернення до джерела» (не потребують фетчу), а veto-гілка того самого
  `alt`-блоку порівнює з уже фетчнутим значенням (потребує). Нарізка
  переїхала на рівень гілок потоку, а не файлів — і саме це відкрило
  реальний паралелізм у хвилі 2 (S-2 fetch-пайплайн ‖ S-3
  allowlist/verify-гейт), якого в лінійному `tg-assistant` не було
  структурно можливо.
- **`tg-assistant` дав 4 хвилі без жодної паралельної пари — і це не
  недогляд.** `sad.md` §5 прямим текстом каже «чотири модулі виконуються
  послідовно (collector → filter → labeler → reporter)». Перша спокуса —
  вигадати штучний паралелізм, щоб хвилі «виглядали як хвилі». Замість
  цього `_generation.md` явно називає властивістю пайплайна: кожна хвиля
  тут = 1 story, чесно, без маскування.
- **Fail→regenerate на Stage 2 (S-2 tg-assistant, gate #5) спіймав
  реальний пропуск, не лише формальну нестачу рядків.** Чернетка
  checklist мала 2 кроки замість мінімум 3 — але справжня проблема була
  не в кількості: жоден крок не реалізовував AC-04 (дедуп повідомлення,
  що підпадає під кілька тем одразу), лише AC-03 (відсіяти шум). Гейт
  спрацював за призначенням: примусив дописати крок, якого бракувало по
  суті, не просто по формату.
- **Порівняння `sdlc-task-packages` vs власний `tasks-forge`** — на
  відміну від `contract-forge` (де вихід вийшов структурно ідентичним на
  worker-поверхні), тут порівняльний прогін цього разу не робився: план
  сесії не включав його в git-стратегію (лише 5 комітів: tg-assistant,
  rules-change-monitor, CONTEXT.md, tasks-forge, docs-оновлення). Скіл
  написаний з власними дефолтами (вивід у `docs/features/`, reference-
  шляхи репо, `events.md`/`openapi.yaml` обидва, `.size`-based хвилі), але
  без живого порівняльного прогону на жодній фічі — чесно назвати цю межу,
  не приховувати відсутність порівняння.
