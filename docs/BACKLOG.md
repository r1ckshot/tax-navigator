# BACKLOG — черга задач

Відповідає на одне питання: **що робити далі.** Кожен рядок у NOW/NEXT — дія, яку
можна почати сьогодні, а не тема для роздумів.

- **NOW** — робиться зараз або відразу наступним.
- **NEXT** — готове до старту, блокерів немає.
- **LATER** — потрібне, але не зараз.
- **Чекає модуля** — не думаємо про це, поки не увійшли в модуль.

Зроблене звідси **прибирається**, не закреслюється: історія є в git і [JOURNAL.md](JOURNAL.md).

---

## NOW

| Задача | Чому | Хто |
|---|---|---|
| T1+T2: почати білд S-1 (collector) з [tasks/tracker.md](features/tg-assistant/tasks/tracker.md) | SDLC-документація завершена, реального коду ще нема. Це новий канал вимірювання G1, а не фіча «колись». Критерій зупинки вже в [PRD](features/tg-assistant/PRD.md) §7: 6 циклів, <5 питань → півот | Claude + Mike |

## NEXT

| Задача | Чому | Хто |
|---|---|---|
| Витрати в інкубаторі: віднімати чи ні | Спливло разом із правкою ричалту 2026-08-05. Інкубатор фактичних витрат від «на руки» не віднімає; `zlecenie` теж, але там це ймовірно правильно — KUP 20%/50% це податкова фікція, а не витрати виконавця. Рішення в DECISIONS 2026-08-05 уже сказало, що «на руки» = гроші в кишені для всіх форм, тож питання лише в тому, чи має інкубатор реальні витрати понад абонемент | Claude + Mike |

## Курс

Окремо від NOW/NEXT свідомо: курсові вправи й продуктова робота конкурували в
одній черзі, і продукт програвав. Тут — що з якого уроку лежить у репо і в якому
стані. Артефакт вважається зданим, коли він **працює в цьому проєкті**, а не коли
написаний.

| Модуль | Артефакт у репо | Стан |
|---|---|---|
| M5, уроки 5.1–5.7 | `.claude/commands/scaffold-rule.md`; скіли `add-source-domain` (+`evals/`), `scenario-tests`, `feature-ship`; команда `/scaffold-scenario`; хуки `block-env-writes`, `block-force-push-master`, `session-stale-rules`, `remind-state-update`, `pre-commit-gate`, `layer-boundary`; плагін `tax-navigator-toolkit/`; [team-marketplace](https://github.com/r1ckshot/team-marketplace); `scripts/state-checkpoint/` | ✅ здано. Кожен артефакт із ізольованим тестом і регресійною перевіркою мутацією; що з цього не видно з коду — [JOURNAL.md](JOURNAL.md), записи 2026-07-30 → 2026-08-02 |
| M5 capstone | [CAPSTONE_LOG.md](../CAPSTONE_LOG.md) + `tax-navigator-toolkit@1.0.0` у публічному marketplace + дві фічі того самого зрізу (F1 руками, F2 через плагін) | ✅ здано 2026-08-04: 44 хв → 18 хв (2.4×), мутації 2/5 → 7/7, хуки спрацювали |
| M5.6 (ідея v1.1) | `bootstrap-agentic-workflow` skill — скаффолдить STATE/BACKLOG/DECISIONS-конвенцію в новий репо, будь-який стек | ⬜ ідея, не спроєктовано; не блокувала реліз v1 |
| M6 SDLC | PRD → arc42+ADR → `tasks/` для `tg-assistant` | ⏳ PRD і SAD здані (6.3, 6.4), далі `tasks/` |
| M6, урок 6.1 | Скіл `.claude/skills/sdlc-audit/` (інспекційний, read-only) + ручна інвентаризація [features/nierejestrowana/artifact-inventory.md](features/nierejestrowana/artifact-inventory.md) + прогін і порівняння [sdlc-audit-run.md](features/nierejestrowana/sdlc-audit-run.md) | ✅ здано 2026-08-05: прогін на реальній фічі дав 7 правок у SKILL.md, з них 3 — з помилок самого скіла |
| M6, урок 6.2 | Готовий (невендорений) `interview`+`fix-term` вручну на новій фічі [rules-change-monitor](features/rules-change-monitor/idea-brief.md) (CONTEXT.md + 15-секційний бриф, Approach C, RICE=12.5) + toy-вправа на іграшковій ідеї (скріншоти, не закомічено) + порівняння з власним `.claude/skills/interview/` | ✅ здано 2026-08-05: порівняння й побічна знахідка (`check-stale-rules.mjs` вже існує) — [JOURNAL.md](JOURNAL.md) |
| M6, урок 6.3 | Адаптовано `.claude/skills/write-prd/`+`prd-review`+агент `prd-critic`; PRD для [tg-assistant](features/tg-assistant/PRD.md) (4 раунди рев'ю) і [rules-change-monitor](features/rules-change-monitor/PRD.md) (`--reference`, Add edge case, 7 раундів); власний скіл `.claude/skills/spec-forge/` + порівняльний прогін [PRD-spec-forge.md](features/tg-assistant/PRD-spec-forge.md) | ✅ здано 2026-08-06: критик двічі спіймав власні арифметичні помилки автора (перерахунок 26 записів `rules.2026.json`); named-states правило `spec-forge` упіймало на чернетці той самий розрив, що `write-prd` знайшов лише через критика |
| M6, урок 6.4 | Адаптовано `.claude/skills/architecture-design/`+агент `sad-critic`; SAD для [tg-assistant](features/tg-assistant/sad.md) (§1-§5, 3 ADR, Edit-стовп із посиланням на §2-інцидент) і [rules-change-monitor](features/rules-change-monitor/sad.md) (перекласифіковано S→M, §1-§12 + повний критик-раунд, 3 ADR, 6 знахідок виправлено); власний скіл `.claude/skills/sad-forge/` (4-й критерій радіусу удару, рекомендовані контейнери репо) + порівняльний прогін [sad-sad-forge.md](features/tg-assistant/sad-sad-forge.md) | ✅ здано 2026-08-06: критик rules-change-monitor зловив дрімаючий AC-10 і самосуперечність в ADR-0003 — деталі [JOURNAL.md](JOURNAL.md) |
| M6, урок 6.5 | Готові (невендорені) `complete-sequence-diagrams`+`generate-data-model` вручну: §6-потік + `data-model.md`+staged-міграції для [tg-assistant](features/tg-assistant/data-model.md) (простий рівень) і повне закриття 10/10 AC + breaking change expand/backfill/contract + roundtrip up→down→up на [rules-change-monitor](features/rules-change-monitor/data-model.md) (складний рівень); власний скіл `.claude/skills/migrations-forge/` (SQLite замість курсового Postgres, власний `node:sqlite`-раннер, contract-фаза = table-rebuild) + порівняльний прогін [data-model-migrations-forge.md](features/tg-assistant/data-model-migrations-forge.md) | ✅ здано 2026-08-07: живий roundtrip-тест зловив реальну межу курсового скіла — `ALTER COLUMN SET NOT NULL` у SQLite не існує; самокорекція протоколу підтвердження діаграм посеред прогону — деталі [JOURNAL.md](JOURNAL.md) |
| M6, урок 6.6 | Готовий (невендорений) `api-forge` вручну: на [tg-assistant](features/tg-assistant/contracts/api-sync-report.md) (простий рівень) — `target_surfaces` не задекларовано, PRD «без публічного API» → skip-гілка, без `openapi.yaml`; на [rules-change-monitor](features/rules-change-monitor/contracts/events.md) (складний рівень, `target_surfaces: ["worker"]`) → `events.md` замість `openapi.yaml`, drift-check 4/4, `--reconcile`-демо (`diff_percent`); власний скіл `.claude/skills/contract-forge/` (`events.md`/`cli.md` за замовчуванням, auth/pagination/codegen opt-in) + порівняльний прогін [events-contract-forge.md](features/rules-change-monitor/contracts/events-contract-forge.md) | ✅ здано 2026-08-10: обидві фічі репо виявились не-HTTP — codegen-крок хард-рівня (oapi-codegen/openapi-typescript) N/A, задокументовано, не пропущено мовчки |
| M6, урок 6.7 (останній у серії) | Готовий (невендорений) `sdlc-task-packages` вручну: на [tg-assistant](features/tg-assistant/tasks/) (простий рівень) — 4 stories/4 хвилі, одна на модуль лінійного пайплайна, чесно без паралелізму (`sad.md` §5 «послідовно»), один навмисний fail→regenerate на Stage 2; на [rules-change-monitor](features/rules-change-monitor/tasks/) (складний рівень) — 5 stories/4 хвилі з реальним паралелізмом (нарізка по гілках `alt`-блоку `diff.mjs`, не по файлах), 8-gate на кожній story; доповнений [CONTEXT.md](features/rules-change-monitor/CONTEXT.md) (5 секцій); власний скіл `.claude/skills/tasks-forge/` (вивід у `docs/features/`, reference-шляхи репо, `events.md`/`openapi.yaml` обидва, `.size`-based waves) | ✅ здано 2026-08-10: capstone-рівень знайшов, що один файл (`diff.mjs`) відповідає за кілька станів із РІЗНИМИ реальними залежностями — нарізка по гілках потоку, не по файлах-модулях |

| M7, урок 7.2 | Демо `7.2-ralph-loop` оглянуто без запуску (RED не відтворюється тут — `uv`/`pytest` відсутні, `environment-limits.md`); власний `PROMPT.md` — реальний Step 5 з [S-1-tg-assistant.md](features/tg-assistant/tasks/S-1-tg-assistant.md) (roundtrip staged-міграцій), не kata-задача; `scripts/verify-tg-assistant-migrations.mjs` + `scripts/test-verify-tg-assistant-migrations.sh` | ✅ здано 2026-08-12: канонічний `ralph.sh` і плагін `ralph-loop` обидва нежиттєздатні в цій сесії (дедлок / hook підхоплюється лише наступною сесією) — цикл (1 ітерація з ліміту 5) виконав сам агент вручну; S-1 Step 5 відмічено done |
| M7, урок 7.3 | Демо `7.3-goal` оглянуто (`make demo`/`make demo-weak` прогнані реально, `make verify` — той самий `uv`-блокер); власна робоча+слабка умова на реальному repo-гепі (порядок сценаріїв на лендингу й у `ComparisonTable`); мета-промптинг + реальний прогін на S-1 Step 3 — [state.ts](../research/tg-assistant/state.ts) | ✅ здано 2026-08-12: `/goal` — клієнтський slash-command, не tool (Claude не може ввести сам, `environment-limits.md`); на робочій умові оцінювач тримав буквальний текст навіть після live human override; на складному рівні прогін виявив, що `research/` був бланкетно в `.gitignore` — жоден файл `tg-assistant` не міг потрапити в git до фіксу |
| M7, урок 7.4 | Демо `7.4-dynamic-workflows` оглянуто (`make check-workflow` зелений, три примітиви підтверджені в `ship-snippets.mjs`, два в `audit-independence.mjs`); на собі — три реальні NEXT-задачі трьома паралельними `Agent`-викликами в одному ході, нуль перетину файлів; складний рівень — `.claude/commands/audit-i18n-safety.md` | ✅ здано 2026-08-13: нативний `/workflows` недосяжний з-під SDK-сесії (той самий клас, що `/goal`, `environment-limits.md`); обхід — окремий термінальний `claude`, підтверджено робочим; живий прогін із фазами й порівнянням токенів не знято, Mike перейшов до візуального рев'ю коміту |
| M7, урок 7.5 | Демо `7.5-background` оглянуто (`make matrix` зелений, `make verify` — той самий `uv`-блокер); обидва рівні — живий `/loop`-аналог через `CronCreate` (GitHub issue-тріаж + git-watchdog на розсинхрон STATE.md), 4 реальні цикли, самозупинка лічильником | ✅ здано 2026-08-13: `/loop`/`/schedule` недосяжні з-під SDK-сесії напряму (той самий клас, `environment-limits.md`), але `CronCreate` — реальний tool-еквівалент; watchdog зловив живий приклад дрейфу (незакомічений `environment-limits.md`, STATE.md про це мовчав); нова знахідка — `tax-navigator-red.vercel.app` недоступний через anycast-дрейф ipset (той самий патерн, що `tax.gov.ua`); дашборд `claude agents` не бачить внутрішніх тіків `CronCreate` — лише окремі сесії |
| M7, урок 7.6 | Демо `7.6-feedback-loops` оглянуто (`make verify` RED на story-28 test-first, `make gate` RED, скіли `verify-ui`/`verify-gate`/`code-reviewer-subagent`, `baselines/` переглянуті); простий рівень — story-26 (відступ кнопки, критерій `class` містить `px-3`), fallback-канал jsdom+RTL замість живого Playwright; складний рівень — story-27 (localStorage-регресія, критичний шлях), self-check DONE проти clean-context рев'юера REJECT→ACCEPT | ✅ здано 2026-08-14: `cdn.playwright.dev` заблокований навіть після firewall-фіксу й Rebuild — флап на рівні edge, не сталий mismatch (третій anycast-кейс, `environment-limits.md`); self-check проґавив гард `queue.length>0`, що ламав AC-1 саме на останній картці, рев'юер зловив з першого прогону; побічна знахідка — story-25 (off-by-one) і story-27 зчеплені, чергу неможливо спорожнити звичайним UI-кліком |
| M7, урок 7.7 (останній у серії M7) | Демо `7.7-tdd-discipline` оглянуто (`make test` RED заблокований — `uv`/`pytest` відсутні, той самий блокер що 7.2-7.5; `make demo` прогнано); простий рівень — реальний AC-10 (S-1 Step 4, backfill window) вручну, 2 коміти (RED/GREEN), `research/tg-assistant/window.ts`; складний рівень — власні `.claude/agents/tdd-{test-writer,implementer,refactorer}.md` + `.claude/skills/tdd/SKILL.md` під vitest/TS (адаптація демо: колоковані `*.test.ts`, обов'язковий `Co-Authored-By`-gate), 2 реальні історії з S-1 (AC-08 retry-queue, AC-02 chat-filter) кожна прогнана і через `/tdd`-агентів, і через одну сесію на окремих гілках | ✅ здано 2026-08-14: нові `.claude/agents/*.md` не підхопились цієї сесії (відомий env-limit) — ізоляцію відтворено через `general-purpose` з вбудованими інструкціями; `--review-tests` зловив реальний дефект (AC-02.2 дублював AC-02.5); власний Gate 3 у SKILL.md мав баг (Python `_underscore`-конвенція замість camelCase цього репо) — знайдено й виправлено живим прогоном. Порівняльні метрики і mini-essay здані як текст на завдання, не як файл у репо |

| M8, урок 8.4 | `make inspect` (Context7, 2 tools); чекліст довіри (5 пунктів) + матриця MCP vs CLI на реальній задачі (Next.js route handler lookup) — вердикт: підключати Context7 | ✅ здано 2026-08-17: `context7.com` не в allowlist (`fetch failed`) — реальний endpoint `context7.com/api/*`, не `mcp.context7.com` з README пакета; додано у фаєрвол (Vercel anycast, той самий патерн що `tax-navigator-red.vercel.app`); `npx @modelcontextprotocol/inspector@2.2.0 --cli` зламаний у цьому контейнері (виконує JSON-RPC payload як shell-команду) — обхід: пін на `@1.0.1`, задокументовано в `environment-limits.md` |
| M8, урок 8.5 | `claude mcp serve` — простий рівень: `tools/list` (24 tools), `resources/list` (`-32601`), `tools/call Read` без підтвердження; складний рівень: `claude mcp add my-claude -- claude mcp serve` self-connect | ✅ здано 2026-08-17: той самий Inspector 2.2.0 CLI-баг що 8.4; вкладений `claude mcp serve` з-під Bash безпечний з `env -u CLAUDE_CODE_*`-обхідним прийомом (на відміну від `claude -p`, дедлоку не було) |
| M8, уроки 8.6-8.8 | Mike сам, у демо-теках курсу (не в репо): 8.6 — 17 тестів + `delete_task` + `outputSchema`/`structuredContent`; 8.7 — Inspector UI/CLI, підкладений баг у `server.buggy.ts`, контрактні однорядники з exit-кодами, фікс порожнього `catch`; 8.8 — web-chat канал, правка `meta`/`instructions`, дебаг із трьох точок | ✅ здано 2026-08-18/19: `DISABLE_TELEMETRY=1` глушить preview-фічі — канал 8.8 не піднімався, поки змінна стояла (побічний ефект, не код демо) |
| M8, capstone | Власний сервер + канал на домені репо: [mcp/evidence-guard/](../mcp/evidence-guard/) (3 tools + resource + prompt, `outputSchema`, зламаний двійник, канал `claude/channel` з `POST /webhook`, секрет + `strictObject`-allowlist + санітизація), звіт і виміри — [capstones/m8.md](capstones/m8.md) | ✅ здано 2026-08-19: 50 тестів + Inspector/curl-докази; у живій сесії подія з вебхука сама виринула тегом `<channel>`, Claude пішов по `get_rule`, звірив із живим `zus.pl` і закрив її `ack_event`. Два гейти каналу: фіча-флаг, який ховає `DISABLE_TELEMETRY=1` (issue 45918, env підхоплюється лише на старті сесії), і запуск не з теки пакета. Другий запис у `.mcp.json` відкинуто — при зайнятому порті давав `CONNECTION_CLOSED` на весь сервер |
| M9, урок 9.1 | `CLAUDE.md` §Git — правило раннього draft PR; чек стейджингу `.env` у [`pre-commit-gate.mjs`](../.claude/hooks/pre-commit-gate.mjs) (канал агента) + git-рівневий [`.githooks/pre-commit`](../.githooks/pre-commit) (індекс, будь-хто) з ізольованими тестами 13+8 кейсів; реальна зміна коротким потоком — 5 атомарних комітів у `feat/git-hygiene-9-1`, draft PR [#7](https://github.com/r1ckshot/tax-navigator/pull/7) | ✅ здано 2026-08-21: агентський хук закриває лише власний канал — `git add -f .env`, набраний людиною в терміналі, лишався відкритим і зажадав другого шару; git не має хука на `git add` взагалі, тож git-рівневий шар ловить уже індекс на `git commit`. Побічно знайдено дірку в парсері гейта (вердикт по першому git-сегменту: `git log && git commit` проходив повз усі чеки) і причину відсутнього `gh` (apt-джерело в `Dockerfile` вказувало на pool-шлях `/packages/stable/deb` замість кореня) |
| M9, урок 9.2 | [`.worktreeinclude`](../.worktreeinclude) (4 патерни + блок «свідомо НЕ копіюємо»); `.gitignore` — `**/`-якорі локального tier + `.claude/worktrees/`; ізоляція портів [`scripts/worktree-ports.mjs`](../scripts/worktree-ports.mjs) (10 тестів) підключена до `npm run dev`/`start` і до `PORT` у `mcp/evidence-guard/Makefile`; два канали одночасно на 8790 і 8798, гілка `feat/worktree-hygiene-9-2`, draft PR [#8](https://github.com/r1ckshot/tax-navigator/pull/8) | ✅ здано 2026-08-21: патерн зі слешем усередині якориться на корінь репо, тож `**/.claude/settings.local.json` — не косметика, без нього вкладений `mcp/evidence-guard/.claude/` висів untracked; `.claude/worktrees/` доти виключав лише `.git/info/exclude`, який у клон не їде. Найдорожче не порти, а залежності: `npm ci` у свіжому worktree 377 с на 9p, тому обрано `worktree.symlinkDirectories` замість копії. `dist/` і `data/` свідомо поза `.worktreeinclude` — гучний збій кращий за тиху несвіжість і за розхід аудит-логу. Побічно заміряно холодний імпорт MCP SDK на 9p (17,8 с), через який канал виглядає зламаним при короткому таймауті. Обидва механізми перевірені живим worktree: симлінки і копії створюються автоматично, `.worktreeinclude` читається з головного репо, а не з гілки worktree; при цьому `node_modules/` зі слешем не матчить симлінк, тож патерн виправлено на `node_modules` |
| M9, урок 9.3 | Три шари захисту `master`: звичка в `CLAUDE.md` §Git, хук [`block-force-push-master.mjs`](../.claude/hooks/block-force-push-master.mjs) на будь-який push у `master`/`main` (27 кейсів), GitHub-ruleset `pull_request` + `non_fast_forward` + `deletion`; merge-політика записана в [DECISIONS.md](DECISIONS.md); два worktree на спільному рядку `CLAUDE.md` § Git → реальний конфлікт → вливання по одній із `git diff master..<branch>`; PR [#10](https://github.com/r1ckshot/tax-navigator/pull/10), [#11](https://github.com/r1ckshot/tax-navigator/pull/11), [#12](https://github.com/r1ckshot/tax-navigator/pull/12) | ✅ здано 2026-08-21: перший merge-коміт в історії репо (`4d96003`) — 9.1 і 9.2 злиті ребейзом, тож рубрика капстоуна не мала предмета. Шар 2 і шар 3 доводяться різними каналами: агентський push помирає на хуку і до GitHub не долітає (`BLOCKED: прямий push у захищену гілку "master"`), той самий refspec із термінала долітає і повертає `GH013 ... Changes must be made through a pull request`. Побічно 9.2 виявився недобудованим для комітів: гейт визначав гілку по кореню сесії, тож КОЖЕН коміт із worktree читався як коміт у master, а `docs/capstones/` (в `.gitignore`, але з посиланнями зі STATE/BACKLOG) валив `verify` шістьма мертвими лінками |
| M9, урок 9.4 | Секція `## Pull requests` у [CLAUDE.md](../CLAUDE.md) (title = subject майбутнього merge-коміта, шість секцій тіла з `N/A — <причина>` замість видалення, `Closes #N`, таблиця чотирьох команд `gh` із дозволом на кожну, п'ять запобіжників); перший файл у `.github/` — [`pull_request_template.md`](../.github/pull_request_template.md); дозволи розведені `allow`-create / `ask`-merge у `.claude/settings.json`; прогін власного правила наскрізь — issue [#14](https://github.com/r1ckshot/tax-navigator/issues/14) → гілка `feat/pr-conventions-9-4` → draft PR [#15](https://github.com/r1ckshot/tax-navigator/pull/15) з `Closes #14` після першого коміта | ✅ здано 2026-08-22: `ask` б'є `allow`, тож останні людські ворота перед `master` не знімає навіть ширший патерн на `gh`, який колись заїде в `allow` — ruleset вимагає PR, але не людину, а хуки дивляться на коміт, не на зведений diff гілки. Знахідка про середовище: `permissions` у `.claude/settings.json` підхоплюються **серед сесії** (шість `Bash(gh …)`, вставлені під час роботи, одразу пропустили `gh pr create` без промпту), на відміну від `hooks` — межа проходить по ключах, не по файлу; `environment-limits.md` три тижні стверджував протилежне про весь файл. Побічно `check-anchors` зловив, що карта архітектури цитувала рядок «CI і решта PR-флоу заплановані з Module 9», який ця ж зміна зробила наполовину неправдивим |
| M9, урок 9.5 | Кастомізована копія вбудованого `/security-review` — [`.claude/skills/product-safety-review/`](../.claude/skills/product-safety-review/SKILL.md): три P0 під ризик цього продукту (цифра без `source_url`+`verified_at`, текст-порада замість інформування, браузерний глобал чи npm-імпорт у `app/lib/calc/`), кожен прив'язаний до вже чинного правила репо; дифф читається з робочого дерева, не з `origin/HEAD...`; звіт лягає у [`docs/reviews/`](reviews/2026-08-22-feat-review-stack-9-5.md), застосування — окремим свіжим проходом. Issue [#18](https://github.com/r1ckshot/tax-navigator/issues/18) → гілка `feat/review-stack-9-5` → PR [#19](https://github.com/r1ckshot/tax-navigator/pull/19) | ✅ здано 2026-08-22: generic-прохід на тій самій мутації дав **0 знахідок** — не через слабкість, а через threat-model: інʼєкцій, секретів, PII і authz у клієнтському калькуляторі немає, а його ж false-positive-precedents прямо виключають клієнтський TS. Детермінований шар зловив 2 з 3 P0 і жодного разу за суттю («файл торкається браузера», «число не збіглося з еталоном»), а текст-пораду не бачить **жоден** автоматичний шар репо. Найдорожче знайшов не мій прохід, а людський `/code-review`: сім дефектів у самому правилі рев'ю, серед них дірка в `product-safety.md` — `paths:` вказував на неіснуючу `app/api/**` і не чіпляв `app/lib/i18n/uk.ts`, тобто правило про заборону порад не покривало жодного файлу з текстом. `/code-review` має `disable-model-invocation`: харнес забороняє і виклик через `Skill`, і відтворення його роботи іншими засобами — розподіл «агент рев'ює P0, людина рев'ює коректність» тут нав'язаний інструментом, не обраний |
| M9, урок 9.6 | [`AGENTS.md`](../AGENTS.md) у корені — крос-тульний дім правил рев'ю: таблиця «рушій → файл правил» по самих файлах, `## Review guidelines`, P0/P1/P2 і три P0 з 9.5 одним рядком кожен (повні списки лишаються в скілі); два workflow у [`.github/workflows/`](../.github/workflows) від `/install-github-app` — `claude.yml` на згадку `@claude`, `claude-code-review.yml` зі звуженим тригером (`ready_for_review`, не draft, `paths` на код замість дефолтного `synchronize`); секрет `CLAUDE_CODE_OAUTH_TOKEN`. Issue [#20](https://github.com/r1ckshot/tax-navigator/issues/20) → гілка `feat/platform-layer-9-6` → PR [#21](https://github.com/r1ckshot/tax-navigator/pull/21) | ✅ здано 2026-08-22: на навмисному P0-1 (PR [#23](https://github.com/r1ckshot/tax-navigator/pull/23), закритий без мержу) Copilot і Claude назвали ту саму цифру в тому самому рядку, обидва з посиланням на правило репо — Copilot ужив саме наш номер `P0-1`, тобто `AGENTS.md` реально читається рушієм, який `CLAUDE.md` не бачить. Дві поправки по дорозі: порожній `requested_reviewers` після `POST` не означає «Copilot недоступний» (рев'ю приходить окремою подією), а зелений джоб `claude-code-action` не означає, що рев'ю було — екшн пропускає себе, якщо workflow-файл у PR розходиться з версією на `master` |
| M9, урок 9.7 | [`CHANGELOG.md`](../CHANGELOG.md) (Keep a Changelog, шість категорій, 264 коміти згорнуті в `[0.1.0]`), release notes мовою продукту [`docs/releases/0.1.0.md`](releases/0.1.0.md), чесний semver `0.0.0` → `0.1.0` і [`release.yml`](../.github/workflows/release.yml) — на merge тегує, курує нотатки й `CHANGELOG` через headless `claude -p` ([`prompts/release-notes.md`](../.github/prompts/release-notes.md)) і публікує реліз; changelog іде окремим Release-PR, у `master` workflow не пушить. Issue [#25](https://github.com/r1ckshot/tax-navigator/issues/25) → PR [#26](https://github.com/r1ckshot/tax-navigator/pull/26) → реліз [v0.1.0](https://github.com/r1ckshot/tax-navigator/releases/tag/v0.1.0) | ✅ здано 2026-08-22: [прогін на merge](https://github.com/r1ckshot/tax-navigator/actions/runs/32591945175) зелений, тег і реліз опубліковані. Крок курування на тому прогоні впав і віддав роботу запобіжнику (`continue-on-error` + fallback на переглянуті нотатки) — дві причини знайдені й виправлені (issue [#27](https://github.com/r1ckshot/tax-navigator/issues/27)), виправлений виклик доведений локальним прогоном на 61 коміті |
| M9, капстоун | Одна реальна фіча крізь усю петлю М9: візуальна регресія екрана результату на CI — [playwright.config.ts](../playwright.config.ts), [visual/](../visual/README.md) (3 сцени × 4 проєкти = 12 еталонів), [visual.yml](../.github/workflows/visual.yml) на `ubuntu-24.04`. Worktree → 8 атомарних комітів → draft PR з `Closes #29` → `/security-review` → рев'ю → merge → cleanup. Issue [#29](https://github.com/r1ckshot/tax-navigator/issues/29), PR [#30](https://github.com/r1ckshot/tax-navigator/pull/30) | ✅ здано 2026-08-25: харнес окупився **до** того, як його еталони потрапили в git — перший прогін показав, що екран результату на 375px має ширину 384, тобто їде горизонтально: підпис `Чистими, zł/міс` не влазив у пару з найширшим числом (337px потрібно проти 293 доступних). Рішення «три сходинки замість чотирьох» від 2026-08-04 трималося саме на цьому непоміченому виносі, тобто ухвалювалось проти верстки, якої не існувало. Друга знахідка — від ока Mike на самому еталоні: іконки ризику в заголовках карток стояли нижче центру назви (квадрат 1,5px, трикутник 2,5px), бо `inline-flex` вирівнюється по базовій лінії, а поправка `0.06em` була написана під flex-центрування в таблиці; після правки всі три в межах ±0,5px. Головний висновок про ворота: артефакти з еталонами віддаються з домену поза allowlist, тож комітить їх людина — і це виправдалось тричі, три набори забраковано до коміту, кожен став би істиною для всіх наступних прогонів |
| M10, урок 10.1 | [`.claude/agents/ro-reviewer.md`](../.claude/agents/ro-reviewer.md) — рев'ювер із `tools: Read, Grep, Glob` (без `Bash`, тобто без запису редиректом) і [інвентаризація удару 2027](features/tax-year-2027/inventory.md), зібрана fan-out-ом: чотири поверхні репо під одним контрактом повернення з 6 пунктів + orchestrator-worker на чотири зрізи файлів без перетину, далі два read-only проходи з чистим контекстом. Issue [#34](https://github.com/r1ckshot/tax-navigator/issues/34) → гілка `feat/fanout-2027-inventory-10-1` → PR [#35](https://github.com/r1ckshot/tax-navigator/pull/35) | ✅ здано 2026-08-25: контракт виміряно на тій самій поверхні тим самим типом воркера — 21 096 символів без нього проти 1 562 з ним, тобто один неконтрактований воркер коштує стільки ж, скільки всі вісім контрактованих разом. Рев'ю з чистим контекстом зловило те, чого не бачив лід: жорстке ім'я `rules.2026.json` усередині гейта `check-anchors` (перейменування завалить `npm run verify`) і два хибні твердження воркерів про джерела. Дві межі середовища заміряні, а не згадані: реєстр агентів читається один раз на сесію (новий `ro-reviewer` не з'явився за 5 спроб протягом 25 хв) — **цей діагноз спростовано на 10.2**, затримка виявилась недетермінованою; а вбудований `Explore` — не гарантія read-only, у нього є `Bash`, і пробник записав файл редиректом без запиту дозволу |
| M10, урок 10.2 | Три власні агенти: [`diff-reviewer`](../.claude/agents/diff-reviewer.md) (`sonnet`, `Read, Grep, Glob, Bash`), [`rules-auditor`](../.claude/agents/rules-auditor.md) (`opus`, без `Bash`, з трьома MCP-інструментами `evidence-guard`), [`env-scout`](../.claude/agents/env-scout.md) (`sonnet`, `memory: project`) + PreToolUse-фільтр **вмісту** Bash-команди [`readonly-bash.mjs`](../.claude/hooks/readonly-bash.mjs) з окремим allowlist на агента (40 ізольованих кейсів) + рев'ю всіх дев'яти агентів як коду. Issue [#36](https://github.com/r1ckshot/tax-navigator/issues/36) → гілка `feat/custom-agents-10-2` → PR [#37](https://github.com/r1ckshot/tax-navigator/pull/37) | ✅ здано 2026-08-25: урок про звужений allowlist закінчився висновком, що самого allowlist не досить — `diff-reviewer` без `Bash` не побачить діффу, а з `Bash` пише редиректом, тож межа переїхала на вміст команди. Питання «чи несе payload ім'я агента» закрито читанням бінарника: `By()` кладе `agent_id`/`agent_type` у базу кожного hookInput, і разом із цим названо, що payload без поля хук пропускає, тобто гарантія умовна. Рев'ю дев'яти дало три тихі знахідки: `explorer` звався read-only з невживаним `Bash`, а в п'яти агентів `model` було відсутнє взагалі. Живий прогін `diff-reviewer` на підкладеному діффі знайшов обидва P0 і зверх того — що вигадане число суперечить верифікованому в EVIDENCE |

**Відкинуто свідомо, щоб не плодити інструменти:** скіл `module-context` (знання
вже в `docs/notes/`, скіл лише дублював би — той самий висновок, що дав бенчмарк
`add-source-domain`) і форматер на `PostToolUse` (спершу треба prettier, якого в
проєкті немає; це нова залежність, а не інструмент).

## LATER

| Задача | Суть |
|---|---|
| Звірити складки ZUS/zdrowotnej для `zakład` укр ФОП | Єдине, що тримає `rangeMonthly: null` у ФОП. EVIDENCE підтверджує лише скалю (KAS відмовив у ричалті), про складки не каже; припускати заборонено. PL-домени відкриті, тож це звичайний `/scaffold-rule` |
| IP Box — дослідити й додати в EVIDENCE | Найчастіше питання IT-B2B, якого в матриці немає взагалі |
| Mały ZUS Plus | Потребує торішньої виручки → зайве питання у free tier; природно лягає в платний |
| Питати добровільну хворобову поза контекстом JDG | `voluntarySickness` тепер впливає і на `zlecenie`, але питання показується лише при `jdgStatus === 'from6to30'`. На медіанному шляху людина його не бачить, а число рахується без складки. Розширення анкети = продуктове рішення Mike, не мовчазна правка |
| Профіль P2 («змішана сім'я») | PROJECT називає його #2 після валідації P1 |
| OG-картинка + бренд результату | Шеринг працює, але лінк виглядає голо — а це маркетингова петля |
| Аналітика воронки | Свідомо відкладена: спершу правильні цифри, потім вимірювання |
| Tax year 2027 | art. 52zr гасне 31.12.2026 → анкета має вміти питати рік. Радіус удару вже описаний: [inventory.md](features/tax-year-2027/inventory.md) — що ламається мовчки, що зашите в код, що впаде голосно, що впреться в дані, яких ще немає |

## Чекає модуля

| Задача | Модуль | Примітка |
|---|---|---|
| Білд движка з живим fetch ставок | M7 | Домени в allowlist; пильнувати пастку цифр ветованої реформи zdrowotnej |
| CI + draft PR + Rule of Two | M9 | CLAUDE.md уже описує PR-флоу як «з M9» |
| Агент-моніторинг змін правил | M10 | Ideation пройдено 2026-08-05 ([idea-brief](features/rules-change-monitor/idea-brief.md), Approach C, RICE=12.5). Effort=4pw в брифі — ймовірно завищено: fetch+staleness (`scripts/fetch-zus-benchmark.mjs`, `check-stale-rules.mjs`) вже готові, лишається лише звірка-diff. **Обрано драйвером капстоуна M10** (2026-08-25): у роботу йде саме нарізка звірки-diff, решта пайплайна лишається тут. Підтвердити на 10.4, коли стане видно, чи ділиться нарізка на file-disjoint володіння |
| Платний tier: PDF-план, Stripe | після G3 | Ціна 79 zł, działalność nierejestrowana |

---

## TG-бот — межа автовідправки

Рішення: **бот пише сам не всюди.** Критерій розділення — чий це простір.

| Контекст | Хто відправляє | Чому саме так |
|---|---|---|
| Власний канал / бот у ЛС | **Бот сам** | Твій простір, люди прийшли до бота свідомо |
| Відповідь людині, яка написала боту | **Бот сам** | Пряме звернення = згода на відповідь бота |
| Коментарі в DOU Polska та інші чужі спільноти | **Чернетка → відправляє Mike** | Там на кону твоя репутація в каналі запуску; авто-постинг у чужу групу читається як спам і часто прямо проти правил групи |

**Перед будь-якою автовідправкою в чужу групу** — прочитати правила цієї групи щодо
ботів і зафіксувати рішення в `DECISIONS.md`. Це не наслідок цього документа.

Етапи T1 (збір) і T2 (розмітка «покрито / біла пляма») — у роботі, вони в NEXT.
T3 (чернетки відповідей) і T4 (автовідправка) **відкинуті на рівні брифу** (§14),
а не відкладені: відповідати людям зараз не задача.

⚠️ Якщо колись дійде до T4 — оновити CLAUDE.md: поточне «read-only до Telegram»
перестане бути правдою, і це має бути свідома правка, а не мовчазний дрейф.
