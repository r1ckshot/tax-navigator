# BACKLOG — черга задач

Відповідає на одне питання: **що робити далі.** Кожен рядок у NOW/NEXT — дія, яку
можна почати сьогодні, а не тема для роздумів.

- **NOW** — робиться зараз або відразу наступним.
- **NEXT** — готове до старту, блокерів немає.
- **LATER** — потрібне, але не зараз.
- **Чекає модуля** — не думаємо про це, поки не увійшли в модуль.

Зроблене звідси **прибирається**, не закреслюється: історія є в git і [JOURNAL.md](JOURNAL.md).

---

## NOW

| Задача | Чому | Хто |
|---|---|---|
| **Екран результату: адаптивність + ясність карток** | M5 capstone закрито (лишився пост у TG — за Mike), і це перша задача після нього: борг свідомо не чіпався ні в F1, ні в F2, щоб не роздути замір. Один корінь — верстка результату не тримає вузький екран. (а) `ComparisonTable` — тритиколонкова `<table>` з `white-space: nowrap` на сумі: на 375px рядок розпихає решту. (б) Підтаблиці в картках центровані з фіксованим `space-6` між колонками. (в) Український тягар ФОП (ЄП+ВЗ у zł, ЄСВ у грн) читається як «на руки»: підпис над таблицею замалий, щоб відділити ЧУЖУ юрисдикцію — Mike прочитав 2 850 zł як дохід, хоча це витрата. Лікується разом: картковий лейаут замість таблиць + видима межа блоку тягаря | Claude + візуальний рев'ю Mike |

## NEXT

| Задача | Чому | Хто |
|---|---|---|
| `tg-assistant` T1+T2: короткий PRD → `tasks/` | Це новий канал вимірювання G1, а не фіча «колись». Розмір S → без arc42 і OpenAPI. Критерій зупинки вже в брифі: 6 циклів, <5 питань → півот | Claude + Mike |
| M6 далі: PRD / arc42+ADR / tasks за мапою модулів | Ideation пройдено, карта архітектури готова — наступна фаза відкрита | Claude |
| Розібратись із рештою розходжень карти (п. 1, 5–7) | Три з семи закриті; лишились подвійна класифікація `i18n/`, тести адаптерів у теці ядра, формулювання про браузерні API і три дрібні неточності — [architecture-map.md](architecture-map.md), §Звірка | Claude |

## Курс

Окремо від NOW/NEXT свідомо: курсові вправи й продуктова робота конкурували в
одній черзі, і продукт програвав. Тут — що з якого уроку лежить у репо і в якому
стані. Артефакт вважається зданим, коли він **працює в цьому проєкті**, а не коли
написаний.

| Урок | Артефакт у репо | Стан |
|---|---|---|
| 5.1 Commands | `.claude/commands/scaffold-rule.md` | ✅ прогнано на `fop.esv_vz` |
| 5.2–5.3 Skills | `.claude/skills/add-source-domain/` + `evals/` | ✅ бенчмарк 100% проти 85% на baseline |
| 5.4 Hooks | `.claude/hooks/block-env-writes.mjs` + ізольований тест | ✅ 21 кейс зелені (додано `$IFS` та inline-інтерпретатор), спрацювання підтверджене вживу |
| 5.4 Hooks (другий) | `.claude/hooks/block-force-push-master.mjs` — блокує force-push у `master` | ✅ адаптація курсового `recipe-5-git-policy.sh`; 16 ізольованих кейсів, зокрема "гілка не названа явно" через fixture-репо |
| 5.4 Hooks (третій) | `.claude/hooks/session-stale-rules.mjs` — SessionStart зі списком протермінованих `verified_at` (>90 днів) | ✅ спільна логіка з `scripts/check-stale-rules.mjs` (nudge, не verify-гейт); 5 кейсів, зокрема межа порогу |
| 5.4 Hooks (п'ятий) | `.claude/hooks/remind-state-update.mjs` — UserPromptSubmit нагадує про коміти, яких `docs/STATE.md` ще не бачив | ✅ ненав'язливо (не Stop — курсовий `stop-quality-gate.sh` сам застерігає, що жорсткий Stop-гейт anti-pattern); 6 кейсів, зокрема укр. плюралізація "1 коміт/3 коміти/5 комітів" |
| 5.4 Hooks (четвертий) | `.claude/hooks/pre-commit-gate.mjs` — блокує коміт з кирилицею в message, замінив інлайн `npm test` на `npm test` + `npm run verify` | ✅ підключено в `settings.json`; спрацювання підтвердиться в новій сесії (правки `settings.json` читаються раз на старті) |
| 5.5 Plugins | `tax-navigator-toolkit/` — конвертація реального `.claude/` (scaffold-rule + add-source-domain + hooks) | ✅ `claude plugin validate` зелений; живий `--plugin-dir` тест зависав у цьому контейнері (environment-limits.md) — структурна перевірка замінює |
| 5.6 Marketplace | [team-marketplace](https://github.com/r1ckshot/team-marketplace) — v1 = `block-env-writes@1.0.0` | ✅ CI зелений, тег `block-env-writes@1.0.0`, install-флоу підтверджений наскрізно (marketplace add → install → живий блок у терміналі Mike) |
| 5.6 Marketplace (v1.1 ідея) | `bootstrap-agentic-workflow` skill — скаффолдить STATE/BACKLOG/DECISIONS-конвенцію в новий репо, будь-який стек | ⬜ ідея, не спроєктовано — окремо від v1, щоб не блокувати реліз |
| M5 capstone | `CAPSTONE_LOG.md` + `tax-navigator-toolkit@1.0.0` у публічному marketplace + дві фічі того самого зрізу (F1 `zlecenie` руками, F2 `nierejestrowana` через плагін) | ✅ усі 5 етапів; 44 хв → 18 хв (2.4×), мутації 2/5 → 7/7, хук спрацював. Лишився пост у TG — за Mike |
| 5.7 SDK | `scripts/state-checkpoint/` — Claude Agent SDK чернетка запису `Закрито` у STATE.md з git log | ✅ форк `sdk-cli`; 3 виміри `--allowed-tools` (Edit звужено до одного файлу); `is_error`-гейт і invalid-prompt тест підтверджені; живий прогін на реальному репо знайшов і виправив дедлок вкладеного `claude -p` (`environment-limits.md`) |
| M6 SDLC | PRD → arc42+ADR → `tasks/` для `tg-assistant` | ⏳ ideation пройдено, далі PRD |

**Відкинуто свідомо, щоб не плодити інструменти:** скіл `module-context` (знання
вже в `docs/notes/`, скіл лише дублював би — той самий висновок, що дав бенчмарк
`add-source-domain`) і форматер на `PostToolUse` (спершу треба prettier, якого в
проєкті немає; це нова залежність, а не інструмент).

## LATER

| Задача | Суть |
|---|---|
| Довести benchmark до 10/10 профілів (**ворота G2**) | Зараз закріплено 1 калібрувальний профіль. Треба 10, прорахованих через держкалькулятори |
| Звірити складки ZUS/zdrowotnej для `zakład` укр ФОП | Єдине, що тримає `rangeMonthly: null` у ФОП. EVIDENCE підтверджує лише скалю (KAS відмовив у ричалті), про складки не каже; припускати заборонено. PL-домени відкриті, тож це звичайний `/scaffold-rule` |
| IP Box — дослідити й додати в EVIDENCE | Найчастіше питання IT-B2B, якого в матриці немає взагалі |
| Mały ZUS Plus | Потребує торішньої виручки → зайве питання у free tier; природно лягає в платний |
| Перевести еталони решти сценаріїв на звірку з центром смуги | `benchmark.test.ts` для JDG/UoP/інкубатора досі спирається на `rangeContains` — а ±4% ковтає похибку в сотні злотих на місяць (знайдено мутаційною перевіркою 2026-08-03 на `zlecenie`). Ті самі тести можуть бути зеленими при зсунутій арифметиці |
| Питати добровільну хворобову поза контекстом JDG | `voluntarySickness` тепер впливає і на `zlecenie`, але питання показується лише при `jdgStatus === 'from6to30'`. На медіанному шляху людина його не бачить, а число рахується без складки. Розширення анкети = продуктове рішення Mike, не мовчазна правка |
| Профіль P2 («змішана сім'я») | PROJECT називає його #2 після валідації P1 |
| OG-картинка + бренд результату | Шеринг працює, але лінк виглядає голо — а це маркетингова петля |
| Аналітика воронки | Свідомо відкладена: спершу правильні цифри, потім вимірювання |
| Tax year 2027 | art. 52zr гасне 31.12.2026 → анкета має вміти питати рік |
| UI-тест на лендинг | `app/page.tsx` не читає жоден тест — три кола правок 2026-08-03 трималися лише на очах Mike. Мінімум: кількість пунктів і їх порядок збігаються з `compareScenarios`. Верстку тест не покриє, але мовчазне розходження списків спіймає |

## Чекає модуля

| Задача | Модуль | Примітка |
|---|---|---|
| Білд движка з живим fetch ставок | M7 | Домени в allowlist; пильнувати пастку цифр ветованої реформи zdrowotnej |
| CI + draft PR + Rule of Two | M9 | CLAUDE.md уже описує PR-флоу як «з M9» |
| Агент-моніторинг змін правил | M10 | Замінює ручну щорічну звірку |
| Платний tier: PDF-план, Stripe | після G3 | Ціна 79 zł, działalność nierejestrowana |

---

## TG-бот — межа автовідправки

Рішення: **бот пише сам не всюди.** Критерій розділення — чий це простір.

| Контекст | Хто відправляє | Чому саме так |
|---|---|---|
| Власний канал / бот у ЛС | **Бот сам** | Твій простір, люди прийшли до бота свідомо |
| Відповідь людині, яка написала боту | **Бот сам** | Пряме звернення = згода на відповідь бота |
| Коментарі в DOU Polska та інші чужі спільноти | **Чернетка → відправляє Mike** | Там на кону твоя репутація в каналі запуску; авто-постинг у чужу групу читається як спам і часто прямо проти правил групи |

**Перед будь-якою автовідправкою в чужу групу** — прочитати правила цієї групи щодо
ботів і зафіксувати рішення в `DECISIONS.md`. Це не наслідок цього документа.

Етапи T1 (збір) і T2 (розмітка «покрито / біла пляма») — у роботі, вони в NEXT.
T3 (чернетки відповідей) і T4 (автовідправка) **відкинуті на рівні брифу** (§14),
а не відкладені: відповідати людям зараз не задача.

⚠️ Якщо колись дійде до T4 — оновити CLAUDE.md: поточне «read-only до Telegram»
перестане бути правдою, і це має бути свідома правка, а не мовчазний дрейф.
