# BACKLOG — черга задач

Відповідає на одне питання: **що робити далі.** Кожен рядок у NOW/NEXT — дія, яку
можна почати сьогодні, а не тема для роздумів.

- **NOW** — робиться зараз або відразу наступним.
- **NEXT** — готове до старту, блокерів немає.
- **LATER** — потрібне, але не зараз.
- **Чекає модуля** — не думаємо про це, поки не увійшли в модуль.

Зроблене звідси **прибирається**, не закреслюється: історія є в git і [JOURNAL.md](JOURNAL.md).

---

## NOW

| Задача | Чому | Хто |
|---|---|---|
| `tg-assistant` T1+T2: `tasks/` | Це новий канал вимірювання G1, а не фіча «колись». Розмір S → без arc42 і OpenAPI. Критерій зупинки вже в [PRD](features/tg-assistant/PRD.md) §7: 6 циклів, <5 питань → півот | Claude + Mike |

## NEXT

| Задача | Чому | Хто |
|---|---|---|
| Прибрати другу особу з UI-копії | 36 рядків зі 184 в `i18n/uk.ts` звертаються на «ти»: «ти вказав», «твоїй виручці», «перевір». Це не лише стиль — `product-safety` забороняє рекомендації, а друга особа сама тягне в них: від «ти платиш» до «тобі краще ричалт» один крок, і стримує його тільки дисципліна. Безособова форма опирається структурно. Кожен рядок бачить користувач, тож задача закінчується очима Mike, а не зеленими тестами | Claude + Mike |
| Витрати в інкубаторі: віднімати чи ні | Спливло разом із правкою ричалту 2026-08-05. Інкубатор фактичних витрат від «на руки» не віднімає; `zlecenie` теж, але там це ймовірно правильно — KUP 20%/50% це податкова фікція, а не витрати виконавця. Рішення в DECISIONS 2026-08-05 уже сказало, що «на руки» = гроші в кишені для всіх форм, тож питання лише в тому, чи має інкубатор реальні витрати понад абонемент | Claude + Mike |
| M6 далі: PRD / arc42+ADR / tasks за мапою модулів | Ideation пройдено, карта архітектури готова — наступна фаза відкрита | Claude |
| Розібратись із рештою розходжень карти (п. 1, 5–7) | Три з семи закриті; лишились подвійна класифікація `i18n/`, тести адаптерів у теці ядра, формулювання про браузерні API і три дрібні неточності — [architecture-map.md](architecture-map.md), §Звірка | Claude |

## Курс

Окремо від NOW/NEXT свідомо: курсові вправи й продуктова робота конкурували в
одній черзі, і продукт програвав. Тут — що з якого уроку лежить у репо і в якому
стані. Артефакт вважається зданим, коли він **працює в цьому проєкті**, а не коли
написаний.

| Модуль | Артефакт у репо | Стан |
|---|---|---|
| M5, уроки 5.1–5.7 | `.claude/commands/scaffold-rule.md`; скіли `add-source-domain` (+`evals/`), `scenario-tests`, `feature-ship`; команда `/scaffold-scenario`; хуки `block-env-writes`, `block-force-push-master`, `session-stale-rules`, `remind-state-update`, `pre-commit-gate`, `layer-boundary`; плагін `tax-navigator-toolkit/`; [team-marketplace](https://github.com/r1ckshot/team-marketplace); `scripts/state-checkpoint/` | ✅ здано. Кожен артефакт із ізольованим тестом і регресійною перевіркою мутацією; що з цього не видно з коду — [JOURNAL.md](JOURNAL.md), записи 2026-07-30 → 2026-08-02 |
| M5 capstone | [CAPSTONE_LOG.md](../CAPSTONE_LOG.md) + `tax-navigator-toolkit@1.0.0` у публічному marketplace + дві фічі того самого зрізу (F1 руками, F2 через плагін) | ✅ здано 2026-08-04: 44 хв → 18 хв (2.4×), мутації 2/5 → 7/7, хуки спрацювали |
| M5.6 (ідея v1.1) | `bootstrap-agentic-workflow` skill — скаффолдить STATE/BACKLOG/DECISIONS-конвенцію в новий репо, будь-який стек | ⬜ ідея, не спроєктовано; не блокувала реліз v1 |
| M6 SDLC | PRD → arc42+ADR → `tasks/` для `tg-assistant` | ⏳ ideation пройдено, далі PRD |
| M6, урок 6.1 | Скіл `.claude/skills/sdlc-audit/` (інспекційний, read-only) + ручна інвентаризація [features/nierejestrowana/artifact-inventory.md](features/nierejestrowana/artifact-inventory.md) + прогін і порівняння [sdlc-audit-run.md](features/nierejestrowana/sdlc-audit-run.md) | ✅ здано 2026-08-05: прогін на реальній фічі дав 7 правок у SKILL.md, з них 3 — з помилок самого скіла |
| M6, урок 6.2 | Готовий (невендорений) `interview`+`fix-term` вручну на новій фічі [rules-change-monitor](features/rules-change-monitor/idea-brief.md) (CONTEXT.md + 15-секційний бриф, Approach C, RICE=12.5) + toy-вправа на іграшковій ідеї (скріншоти, не закомічено) + порівняння з власним `.claude/skills/interview/` | ✅ здано 2026-08-05: порівняння й побічна знахідка (`check-stale-rules.mjs` вже існує) — [JOURNAL.md](JOURNAL.md) |
| M6, урок 6.3 | Адаптовано `.claude/skills/write-prd/`+`prd-review`+агент `prd-critic`; PRD для [tg-assistant](features/tg-assistant/PRD.md) (4 раунди рев'ю) і [rules-change-monitor](features/rules-change-monitor/PRD.md) (`--reference`, Add edge case, 7 раундів); власний скіл `.claude/skills/spec-forge/` + порівняльний прогін [PRD-spec-forge.md](features/tg-assistant/PRD-spec-forge.md) | ✅ здано 2026-08-06: критик двічі спіймав власні арифметичні помилки автора (перерахунок 26 записів `rules.2026.json`); named-states правило `spec-forge` упіймало на чернетці той самий розрив, що `write-prd` знайшов лише через критика |

**Відкинуто свідомо, щоб не плодити інструменти:** скіл `module-context` (знання
вже в `docs/notes/`, скіл лише дублював би — той самий висновок, що дав бенчмарк
`add-source-domain`) і форматер на `PostToolUse` (спершу треба prettier, якого в
проєкті немає; це нова залежність, а не інструмент).

## LATER

| Задача | Суть |
|---|---|
| Звірити складки ZUS/zdrowotnej для `zakład` укр ФОП | Єдине, що тримає `rangeMonthly: null` у ФОП. EVIDENCE підтверджує лише скалю (KAS відмовив у ричалті), про складки не каже; припускати заборонено. PL-домени відкриті, тож це звичайний `/scaffold-rule` |
| IP Box — дослідити й додати в EVIDENCE | Найчастіше питання IT-B2B, якого в матриці немає взагалі |
| Mały ZUS Plus | Потребує торішньої виручки → зайве питання у free tier; природно лягає в платний |
| Питати добровільну хворобову поза контекстом JDG | `voluntarySickness` тепер впливає і на `zlecenie`, але питання показується лише при `jdgStatus === 'from6to30'`. На медіанному шляху людина його не бачить, а число рахується без складки. Розширення анкети = продуктове рішення Mike, не мовчазна правка |
| Профіль P2 («змішана сім'я») | PROJECT називає його #2 після валідації P1 |
| OG-картинка + бренд результату | Шеринг працює, але лінк виглядає голо — а це маркетингова петля |
| Аналітика воронки | Свідомо відкладена: спершу правильні цифри, потім вимірювання |
| Tax year 2027 | art. 52zr гасне 31.12.2026 → анкета має вміти питати рік |
| Візуальна регресія (скріншоти) | Екран результату перебудовано 2026-08-04 за сім кіл правок, і **жоден тест цього не бачить**: зламаний медіазапит лишить набір зеленим. Зараз перевірка неможлива — браузера в контейнері немає (`environment-limits.md`), тож задача чекає CI з M9, де браузер буде. Мінімум: 375px і широкий, світла й темна тема, згорнута й розкрита картка |
| UI-тест на лендинг | `app/page.tsx` не читає жоден тест — три кола правок 2026-08-03 трималися лише на очах Mike. Мінімум: кількість пунктів і їх порядок збігаються з `compareScenarios`. Верстку тест не покриє, але мовчазне розходження списків спіймає |

## Чекає модуля

| Задача | Модуль | Примітка |
|---|---|---|
| Білд движка з живим fetch ставок | M7 | Домени в allowlist; пильнувати пастку цифр ветованої реформи zdrowotnej |
| CI + draft PR + Rule of Two | M9 | CLAUDE.md уже описує PR-флоу як «з M9» |
| Агент-моніторинг змін правил | M10 | Ideation пройдено 2026-08-05 ([idea-brief](features/rules-change-monitor/idea-brief.md), Approach C, RICE=12.5). Effort=4pw в брифі — ймовірно завищено: fetch+staleness (`scripts/fetch-zus-benchmark.mjs`, `check-stale-rules.mjs`) вже готові, лишається лише звірка-diff |
| Платний tier: PDF-план, Stripe | після G3 | Ціна 79 zł, działalność nierejestrowana |

---

## TG-бот — межа автовідправки

Рішення: **бот пише сам не всюди.** Критерій розділення — чий це простір.

| Контекст | Хто відправляє | Чому саме так |
|---|---|---|
| Власний канал / бот у ЛС | **Бот сам** | Твій простір, люди прийшли до бота свідомо |
| Відповідь людині, яка написала боту | **Бот сам** | Пряме звернення = згода на відповідь бота |
| Коментарі в DOU Polska та інші чужі спільноти | **Чернетка → відправляє Mike** | Там на кону твоя репутація в каналі запуску; авто-постинг у чужу групу читається як спам і часто прямо проти правил групи |

**Перед будь-якою автовідправкою в чужу групу** — прочитати правила цієї групи щодо
ботів і зафіксувати рішення в `DECISIONS.md`. Це не наслідок цього документа.

Етапи T1 (збір) і T2 (розмітка «покрито / біла пляма») — у роботі, вони в NEXT.
T3 (чернетки відповідей) і T4 (автовідправка) **відкинуті на рівні брифу** (§14),
а не відкладені: відповідати людям зараз не задача.

⚠️ Якщо колись дійде до T4 — оновити CLAUDE.md: поточне «read-only до Telegram»
перестане бути правдою, і це має бути свідома правка, а не мовчазний дрейф.
