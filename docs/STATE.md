# STATE — де ми зараз

Веде Claude Code, оновлення = частина DoD. Тримати в межах однієї сторінки:
історія йде в [JOURNAL.md](JOURNAL.md), рішення — в [DECISIONS.md](DECISIONS.md), черга задач — у [BACKLOG.md](BACKLOG.md).

## Фаза

**Курс:** M4 здано (`docs/capstones/m4.md`), M5 здано (усі 7 лекцій застосовані
на реальному продукті — `interview`, хуки, `tax-navigator-toolkit`,
`team-marketplace`, `scripts/state-checkpoint/`), M6 у роботі — пройдено
ideation по фічі `tg-assistant`, далі PRD і tasks.

**Продукт:** FREE-анкета в проді на Vercel — вердикт резидентства + порівняння
6 сценаріїв рахуються детерміновано на клієнті. 135 node-тестів + 11 UI зелені.

**Ворота:** G1 перевідкриті (провалився канал, не попит — DECISIONS 2026-07-28).
G2 (10/10 профілів проти держкалькуляторів) не пройдені — закріплено 1 калібрувальний.

## Зараз у роботі

**M5 capstone — усі 5 етапів пройдені, лишився пост у TG.** Вимір і всі рішення —
[CAPSTONE_LOG.md](../CAPSTONE_LOG.md).

Definition of Done рубрики закрито по пунктах:

| Пункт | Стан |
|---|---|
| F1 наскрізний зріз, руками | ✅ `zlecenie`, PR #1 |
| F2 той самий зріз, через плагін | ✅ `nierejestrowana`, PR #3 |
| Плагін у ≥3 ключових кроках F2 | ✅ 4 виклики |
| F2 швидша за F1 щонайменше 2× | ✅ 44 хв → 18 хв (2.4×) |
| Хук реально спрацював, є запис | ✅ `layer-boundary`, `core-no-external` |
| Плагін опублікований публічно | ✅ [team-marketplace](https://github.com/r1ckshot/team-marketplace), тег `tax-navigator-toolkit@1.0.0`, CI зелений |
| README плагіна на 5 секцій | ✅ Purpose / Install / Commands / Skills / Hooks |
| SDK-скрипт: 4 критерії зарахування | ✅ перевірені живим прогоном (Етап 4 у логу) |

**Лишилось Mike:** пост у TG-гілку «Модуль №3» — посилання на marketplace,
скріншот [PR #3](https://github.com/r1ckshot/tax-navigator/pull/3), 2–3 речення
про закритий pain point. Шаблон і цифри — в CAPSTONE_LOG.

### Етап 3 закрито: F2 через плагін

Сценарій **`działalność nierejestrowana`** зроблено через плагін (це предмет
виміру, а не спосіб зекономити). Ланцюг пройдено повністю: `/scaffold-scenario`
→ `/scaffold-rule` → скіл `scenario-tests` → скіл `feature-ship`.

- [x] Чотири правила `nierejestrowana.*` (limit / zus / pit / cudzoziemcy),
  звірені 2026-08-03 з biznes.gov.pl і podatki.gov.pl. EVIDENCE §Сценарій H
- [x] **Знахідка, що змінила сценарій:** «nierejestrowana = без ZUS» вірне лише
  для продажу товарів. За ПОСЛУГИ договір є umową o świadczenie usług, тобто
  zleceniem за k.c. — складки платить замовник. Наївна версія (нуль ZUS) дала б
  завищене «на руки» на ~23%
- [x] Ліміт 10,813.50 zł/квартал = 3,604.50 zł/міс: медіанний профіль анкети
  поза ним учетверо, тож сценарій штатно показує ПРИЧИНУ замість числа (як `fop`)
- [x] Реєстрація в 10 місцях; пункт 10 виявив застарілий від F1 рядок у
  `architecture-map.md` — порядок сценаріїв там лишався `[fop, jdg, incubator, uop]`
- [x] Тести: 135 node + 11 UI зелені, `depcruise` 0 порушень. **7 з 7 навмисних
  мутацій спіймано** (проти 2 з 5 на першій ітерації F1)
- [x] Хук `layer-boundary` підтверджено живим у новій сесії контрольованим
  спрацюванням; на реальних записах не спрацював жодного разу
- [x] Візуальний рев'ю Mike пройдено. Лендинг перебудовано на сітку 2 × 3 з
  вертикальним роздільником і однією колонкою нижче 48rem — попередній
  `flex-basis` розривник ламав рядок не на всякій ширині. Правило дужок у назвах
  (польський термін лише там, де укр. назва його не розкриває) записане в `uk.ts`.
  **Лендинг не покритий тестами** — заведено в BACKLOG → LATER
- [x] Чотири коміти + рядки в [CAPSTONE_LOG.md](../CAPSTONE_LOG.md), включно з
  розділами «Порівняння» і «Де плагін НЕ допоміг»

### Етапи 4–5 закрито (SDK проти рубрики + публікація)
- [x] `--max-turns` 15 → **12**: живий прогін дав `num_turns=10` при `exit=0`,
  тобто два ходи запасу, і 12 тримається в діапазоні 6–12 з рубрики
- [x] Критерій «manual apply» **ніде не перевірявся** — тепер перевірений:
  `git diff > checkpoint.patch` → `git worktree add` → `git apply` без конфліктів
- [x] `tax-navigator-toolkit@1.0.0` у публічному marketplace; CI ганяє **всі**
  `hooks/test-*.sh` плагіна, а не одне зашите імʼя — доти три сюїти toolkit-а
  мовчки не запускались би, а бейдж лишався зеленим

### Наступний крок після capstone

**Екран результату: адаптивність + ясність карток** (BACKLOG → NEXT, перша
задача). Борг свідомо не чіпався ні в F1, ні в F2, щоб не роздути замір.

### Етап 2 закрито (плагін витягнуто з F1)
- [x] Хук `layer-boundary.mjs` — п'ять правил `.dependency-cruiser.cjs` у момент
  запису файлу, а не після `test:arch`. 25 кейсів, 4 мутації ловляться.
  Підключено в `settings.json` (Mike) — **оживе з наступної сесії**
- [x] `/scaffold-scenario` — ланцюг реєстрації з 10 місць, звіт по кожному
- [x] Скіл `scenario-tests` — еталон від центру смуги + мутаційна перевірка
- [x] Скіл `feature-ship` — гейт із 4 перевірок + DoD по пунктах + чернетка STATE
- [x] `tax-navigator-toolkit` v1.0.0: README на 5 секцій, `hooks.json` із новим
  хуком, `claude plugin validate` зелений
- [x] `npm run verify` звіряє 11 пар `.claude/` ↔ плагін побайтово (перевірено
  навмисним розходженням) — копії більше не розійдуться мовчки

### Етап 1 закрито (F1 руками, PR #1 змерджено)
- [x] Сценарій `zlecenie`: правила `zlecenie.contributions` / `.kup` /
  `.zbieg_z_etatem` / `.przekwalifikowanie` (звірені 2026-08-03 з biznes.gov.pl,
  podatki.gov.pl, gov.pl), `calc/scenarios/zlecenie.ts`, реєстрація, i18n,
  рядок у порівнянні. EVIDENCE §Сценарій G
- [x] Еталони виведені вручну і **звіряються з центром смуги, а не через
  `rangeContains`**: мутаційна перевірка показала, що смуга ±4% ковтає похибку
  в 164 zł/міс — 3 з 5 навмисних регресій проходили тест. Після переписування
  ловляться 5 з 5
- [x] Нестабільність #5 в EVIDENCE: biznes.gov.pl називає стелю 50% KUP
  «85,528 zł» — це ліміт ulgi dla młodych, не KUP. Чинна стеля 120,000 zł
- [x] `pre-commit-gate.mjs` полагоджено: поле `if: Bash(git commit*)` у
  settings.json **не фільтрує**, хук ганяв повний `npm test` + `verify` на
  кожній Bash-команді. Гейт перенесено всередину хука + 12 кейсів у
  `test-pre-commit-gate.sh`
- [x] Візуальний рев'ю Mike пройдено. Знайдено неадаптивність таблиць результату
  й нечитабельний блок українського тягаря ФОП — **свідомо не чіпалось у F1**:
  перебудова лейауту роздула б замір роботою, якої в F2 не буде. Перша задача
  після capstone (BACKLOG → NEXT)
- [x] PR #1 змерджено в `master` через rebase, історія лишилась лінійною

Закрито 2026-08-03 (5.4 Hooks — три останні хуки модуля, 2 коміти):
- [x] `block-force-push-master.mjs` — адаптація курсового `recipe-5-git-policy.sh`
  (Node замість bash+python3, токен-парсинг замість одного regex — ловить
  force-флаг незалежно від позиції і випадок "гілку не назвали явно"
  через `git rev-parse --abbrev-ref HEAD`). 16 ізольованих кейсів
- [x] `session-stale-rules.mjs` — SessionStart nudge про правила без
  переверки джерела >90 днів (поріг узгоджено з Mike). Спільна логіка
  винесена в `scripts/check-stale-rules.mjs`, щоб не розійшлась у двох
  місцях. 5 кейсів, зокрема межа порогу (89 vs 91 днів)
- [x] `remind-state-update.mjs` — UserPromptSubmit нагадує, скільки
  комітів `docs/STATE.md` ще не бачив. Свідомо не Stop-хук: курсовий
  `stop-quality-gate.sh` сам застерігає в коментарі, що жорсткий
  Stop-гейт — anti-pattern (може заблокувати посеред задачі). 6 кейсів,
  зокрема укр. плюралізація і виняток mod100=11
- [x] Усі три — з реальною регресійною перевіркою (навмисна мутація коду
  → тест падає → відкат), не лише щасливий шлях
- [x] `settings.json` wiring застосував Mike вручну (Claude Code не
  редагує власний settings.json); live-спрацювання підтвердиться в новій
  сесії
- [x] Урок сесії: ці два коміти замерджені й запушені БЕЗ цього
  STATE.md-запису одразу — DoD не дотриманий по гарячих слідах, виправлено
  окремим комітом після того, як Mike запитав чому

Закрито 2026-08-02 (M5.7 SDK — `scripts/state-checkpoint/`, 2 коміти):
- [x] Форк `sdk-cli/release-notes.sh` (курс 5.7) під реальну задачу: замість
  CHANGELOG.md/git-тегів (яких нема в продукті) — чернетка запису `Закрито`
  у `docs/STATE.md`, межа = останній коміт, що чіпав STATE.md
- [x] Три виміри `--allowed-tools`: `Bash(git log *)`, `Read(docs/**)`,
  `Edit(docs/STATE.md)` — вужче за demo (рівно один файл); JSON-schema на
  structured output; `--max-turns 15` (6/8/10 падали на живих прогонах)
- [x] Живий прогін на цьому ж репо знайшов і виправив 3 баги: дедлок
  вкладеного `claude -p` через колізію `CLAUDE_CODE_SESSION_ID` з
  батьківською сесією (~5 хв D-state, `environment-limits.md`); `set -e`
  ховав diff і summary на error-шляху; замалий `--max-turns`
- [x] Invalid-prompt тест (`--max-turns 1`) підтвердив `is_error: true` +
  exit 1. Окрема знахідка: `is_error: false` не гарантує фактичну точність
  — агент кілька разів повертав валідний за схемою, але вигаданий контент
  (неіснуючий Makefile-таргет, невірна кількість тестів) → trust-модель
  "скрипт ніколи не комітить сам" підтверджена на практиці, не лише в теорії
- [x] `test-precheck.sh` — 5 детермінованих кейсів межі комітів без API;
  підтверджено, що ловить регресію (навмисна мутація → 2 провалені кейси)

Закрито 2026-08-01 (`pre-commit-gate.mjs` — гейт перед комітом):
- [x] Два чеки замість інлайн `npm test` у settings.json: кирилиця в
  `git commit`-команді (включно з heredoc-тілом) → deny одразу; інакше
  `npm test` + `npm run verify`, будь-який провал → deny
- [x] Привід: спроба закомітити `docs: fix stale M5 reference in Наступне` —
  українське слово протекло в subject, Mike зловив очима, не хук
- [x] 3 кейси перевірені локально (allow на реальному зеленому прогоні,
  deny-кирилиця, deny-провалений test) — не лише щасливий шлях
- [x] Підключено в `.claude/settings.json` (Mike); спрацювання підтвердиться
  в новій сесії

Закрито 2026-07-31 (M5.6 Marketplace — публічний [team-marketplace](https://github.com/r1ckshot/team-marketplace)):
- [x] v1 = `block-env-writes@1.0.0`, єдиний реальний плагін — курсові приклади
  (deploy-checklist/pr-review-rules/security-scan) свідомо не копіювались,
  у нас немає їхніх реальних відповідників
- [x] Хук закалений перед публікацією: `$IFS`-нормалізація і детекція запису
  через `node -e`/`python3 -c` (веб-дослідження bypass-технік) — 21 кейс
  зелені, синхронізовано і в `.claude/hooks/`, і в `tax-navigator-toolkit/`
- [x] CI (`validate-plugins.yml`: static-validate + plugin-validate + ізольований
  тест хука) зелений на `main`; version-invariant між `marketplace.json` і
  `plugin.json` — власна перевірка, курсовий шаблон її не мав
- [x] Тег `block-env-writes@1.0.0`; install-флоу підтверджений наскрізно
  (`marketplace add` → `install` → живий блок у терміналі Mike), тестова
  інсталяція прибрана після перевірки
- [x] `auto-flow`-ідея (bootstrap-agentic-workflow) занесена в BACKLOG як v1.1,
  не блокує v1

Закрито 2026-07-31 (M5.5 Plugins — конвертація реального `.claude/` у плагін, `tax-navigator-toolkit/`):
- [x] `.claude-plugin/plugin.json`, копія команди `scaffold-rule` і скіла
  `add-source-domain` без змін усередині — свідомо без `interview`/`map-architecture`,
  бо ті адаптовані з курсу, не наш оригінал. `hooks/hooks.json` — кореневий
  `"hooks"`-об'єкт зі `settings.json` програмно звірений байт-у-байт (окрім одного шляху)
- [x] Єдина заміна на `$CLAUDE_PLUGIN_ROOT`: шлях до `block-env-writes.mjs` —
  скрипт фізично переїжджає з плагіном. Шлях до `environment-limits.md` у
  SessionStart-хуку НЕ чіпав: хуки виконуються з cwd хост-проєкту незалежно від
  джерела плагіна, це не шлях усередині плагіна
- [x] `claude plugin validate --strict` зелений; ізольований тест хука (16 кейсів)
  перепрогнаний з нового розташування — зелений
- [x] Живий `claude --plugin-dir` тест: тривіальний prompt пройшов, реальний
  namespace-виклик `/tax-navigator-toolkit:scaffold-rule` тричі завис у цьому
  контейнері (>130с, `timeout` не вбиває дочірній процес) — задокументовано в
  `environment-limits.md`, верифікація лишилась на structural-перевірці

Закрито 2026-07-31 (машинна звірка документів, `scripts/check-docs.mjs` у `npm run verify`):
- [x] Три перевірки: кількість тестів у STATE.md == фактична (vitest --reporter=json),
  NOW у BACKLOG.md не дублює закрите в STATE.md (звірка backtick-токенів),
  відносні markdown-посилання між git-трекнутими `.md` не биті
- [x] Кожна перевірка підтверджена на реальному провалі (тимчасова мутація
  STATE.md/BACKLOG.md, revert через `git checkout`) — не лише на зеленому шляху
- [x] `npm run verify` і `npm test` зелені після інтеграції

Закрито 2026-07-30 (інструментарій M5 — скіл і хук):
- [x] Скіл `add-source-domain` — процедура + судження «чи варто відкривати домен».
  Прогін проти baseline на 3 задачах × 2 конфігурації × 2 ітерації: 100% проти 85%
- [x] Дублі прибрані — `scaffold-rule.md` більше не переказує процедуру, а вказує на скіл.
  До прибирання дельта була рівно 0: baseline читав команду
- [x] Хук `PreToolUse` проти запису в `.env` через шелл + ізольований тест (16 кейсів)
- [x] Хук підтверджений уживу в новій сесії; скіл прогнаний обома способами виклику

Закрито 2026-07-29 (ЄСВ/ВЗ для ФОП, `feat/fop-figures`, 6 комітів):
- [x] `/scaffold-rule fop.esv_vz`: ЄСВ мін. 1,902.34 грн/міс = 22% × мінімалка 8,647;
  ВЗ 3 гр. = 1% доходу. Деталі — EVIDENCE §Сценарій A і §Нестабільності п. 4
- [x] Курс UAH→PLN не конвертуємо (DECISIONS 2026-07-29)
- [x] `fop.ts` віддає `foreignBurden` двома валютами; `rangeMonthly` лишається
  `null` свідомо — складки ZUS для `zakład` не звірені (заведено в BACKLOG → LATER)
- [x] Еталон у `benchmark.test.ts` + UI-тест на підписані валюти
- [x] `SPEC.md` критерій 6 і карта архітектури переформульовані
- [x] Візуальний рев'ю Mike пройдено (правки: підпис над таблицею, одиниця в рядок,
  уточнений напис порожньої комірки)

Закрито 2026-07-29 (три задачі, три гілки, історія лінійна):
- [x] Розчистка: домени, хуки, консистентність 12 файлів, STATE↔JOURNAL, BACKLOG у чергу
- [x] Карта архітектури (`docs/architecture-map.md`) + вендорений скіл `map-architecture` і агент `explorer`
- [x] Три розходження з карти закриті: одне джерело квантизації, одна назва продукту, машинна перевірка мови UI
- [x] Тести зелені (86 node + 8 UI), `verify` зелений, коміти атомарні
- [x] STATE, BACKLOG і DECISIONS оновлені

Деталі — [JOURNAL.md](JOURNAL.md), рішення — [DECISIONS.md](DECISIONS.md).

## Живі блокери

- **`.claude/settings.json` править лише Mike** — Claude Code не редагує власний
  settings (жорсткий класифікатор). Готую блок, застосовує Mike.
- **`~/.claude/settings.json` читається раз на старті сесії** — правки підхоплює
  лише наступна сесія.
- **`next build` у цьому контейнері не запускаємо** — перевірка через `tsc --noEmit`
  + `npm test` + `npm run test:ui`; продакшн-збірку валідує Vercel.
- **G2 не пройдені** — движок не вважається готовим, поки нема 10/10 профілів.

Решта відомих меж середовища — [.claude/rules/environment-limits.md](../.claude/rules/environment-limits.md).

## Наступне

Порядок узгоджений з Mike 2026-08-03 і **не є алфавітом черги** — він тримається
на воротах, не на модулях курсу. Обґрунтування — [DECISIONS.md](DECISIONS.md),
запис 2026-08-03.

**1. Екран результату: адаптивність + ясність карток.** Дрібна на вигляд, але
пункт (в) — не косметика: Mike прочитав український тягар ФОП (2 850 zł) як
«на руки», хоча це витрата. Продукт показав число так, що воно дало хибний
висновок, і жоден тест цього не бачить. Деталі — BACKLOG → NOW.
⚠️ Всередині задачі є **продуктове рішення**, яке треба ухвалити явно: чи всі
шість сценаріїв рівноважні в таблиці, чи `fop` і `nierejestrowana` (обидва
показують причину замість числа для профілю P1) згортаються під основну. Рішення
обмежує майбутні сценарії → рядок у DECISIONS.

**2. G2: 10 профілів проти держкалькуляторів.** Оголошений блокер перед M7,
закріплений поки **один** профіль. Борг зріс під час capstone: сценаріїв стало 6
замість 5, тобто кожен профіль несе на набір чисел більше. Джерела перевірені
2026-08-03: `zus.pl` і `podatki.gov.pl` віддають `200`.

**3. `tg-assistant` T1+T2** — короткий PRD → `tasks/`. Свідомо ПІСЛЯ G2, не до:
G1 міряє готовність платити за відповідь, а відповідь поки не звірена 10/10.
Критерій зупинки вже в брифі: 6 циклів, <5 питань → півот.

Повна черга — [BACKLOG.md](BACKLOG.md).
